# Vulcan Mobile

> **Note to Claude Code:** When a significant feature is added, a major architectural change is made, or a new dependency/service is introduced, update this CLAUDE.md file to reflect the change before finishing the task. Keep it current.

## Project Overview

Vulcan is an AI-powered automotive diagnostic app for professional technicians. Built with Expo/React Native, a Railway-hosted backend, and the Claude API. Targets iOS and Android.

## Current State & Next Steps

> Keep this section current as work progresses — update it when a feature lands, an action item closes, or priorities shift. New sessions read this first to understand momentum, not just architecture.

**Working and validated:**

- All four modes functional (Ask Vulcan, Diagnose, Inspection Report, OBD2 Scan)
- OBD2 core validated on BOTH platforms: Android (OBDLink MX+ over Bluetooth Classic) and iOS (Veepeak OBDCheck BLE+ over BLE) — connects fast, reads DTCs, streams live data
- Live data tracking is responsive and real-time; full per-vehicle PID selection with categorized picker, presets, US/imperial units, and a status panel for bit-level signals
- DTC database (18,805 codes) with manufacturer-specific lookups, config-mismatch detection, Claude fallback + caching
- Auto-VIN populate across all modes, auto-reconnect for saved adapters
- Vehicle specs retrieval backed by the Supabase spec DB (full Option C: verified hit / honest fallback on miss or DB-down / miss-logging to `spec_miss`); Vehicle Finder removed
- OBDb PID database, persistent Railway Volume storage (confirmed surviving redeploys)
- iOS startup crash resolved (reanimated v4 / worklets misconfig)
- Debug logging gated behind `EXPO_PUBLIC_DEBUG_OBD2`
- **DTC parsing verified accurate on 2011 GMC Sierra 4.8L** — multi-ECU CAN responses correctly parsed (0 phantom codes), P0442 surfaces correctly under Permanent Codes, O2 sensors read ~0.45V (not 115V). See DTC Parsing Architecture section.
- **Diagnostic engine Stage 1 (single-shot assessment):** structured differential with stance, hypotheses + evidence, single next step via `/api/assess`, ring buffer in `Obd2Manager`. Originally shipped as a standalone `/smart-diagnose` route; the **Stage 2A restructure merged it into `/diagnose`** (see the Stage 2A bullet below and the Diagnostic Engine Architecture section). The endpoint, snapshot builder, and schema are unchanged.
- **Cost tracking across all modes:** `/api/ask` and `/api/diagnose` return cost in their responses. The mobile app logs `ask_vulcan` and `diagnose_turn` entries to the on-device diagnostic log with full token/cost breakdown. `updateSessionVehicle()` added to `DiagnosticLogger` for vehicle re-attribution after VIN decode.
- **Vehicle attribution fix:** `updateSessionVehicle()` sweeps every entry in the current session (not just `session_start`) when the Mode 09 VIN resolves post-connect. Gate is raw VIN only — no `vehicle.year` required — so NHTSA decode failures still get attributed by VIN. Sessions are per-connect so the full-session sweep is unambiguous. Pre-2008 / no-Mode-09 vehicles can't self-attribute; see Known Gaps.
- **Intake form keyboard fix (Android):** the Diagnose intake (which since Stage 2A includes the former Smart Diagnose intake) tracks keyboard height and applies extra `paddingBottom` so the mileage/complaint fields stay visible above the keyboard on Android.
- **VIN intake field — adapter ground truth (2026-06-14, unified-flow SB1):** the Diagnose intake VIN field is now seeded/auto-imported from `obd2.getConnectedVin()` (the manager's Mode-09 ground truth, cleared on disconnect), NOT the persisted/overridable `ctxVin` — which had two symptoms: a fresh intake leaking the PREVIOUS vehicle's VIN, and a connected intake not auto-importing the connected VIN. Rule: fresh = empty, connected = the connected vehicle's VIN (incl. a late auto-VIN resolve, via an apply-once effect keyed on `ctxVin` change), never the previous vehicle. Reading `getConnectedVin()` (not a `source`-gated `ctxVin`) also closes the reconnect-flash leak; the effect is gated to `phase === "intake"` so it can never write the connected car's VIN into a saved case envelope during chat (a resumed no-VIN case stays no-VIN). The connect-time mismatch case (tech "keeps entered" a manual vehicle) preserves the typed VIN naturally — keep-entered leaves `ctxVin` unchanged so the import never re-runs. Same `obd2.getConnectedVin()` ground-truth principle as the 2B different-vehicle guard. (First sub-batch of the unified-diagnostic-flow merge; independent correctness fix, shipped ahead of the merge.)
- **Unified diagnostic flow merge (the real mode merge) — arc tracker:** folding the conversational `/api/diagnose` and structured `/api/assess` paths into ONE diagnostic brain that decides talk / reach-for-live-monitoring / conclude each turn (today they fire in parallel into the same thread — two brains — and live monitoring is unreachable from a conversational turn; full investigation in the session notes). Resequenced because the merge's reach-for-monitoring decision needs the flow to SEE whether a vehicle is connected. Sub-batches: **SB1 — VIN intake ground-truth fix: COMPLETE** (above). **SB2 — app-level OBD2 connection + remembered adapter: COMPLETE + validated on a real F-350 (2026-06-14)** (connection lifted to app-level so any screen sees it without visiting the OBD2 screen — see Auto-reconnect under Key Architecture Details: `Obd2Provider` owner + `ensureAutoReconnect` + the `runExclusiveConnect` connect mutex + reactive `Obd2Context.connectedVin`; mobile-only). **SB3 — the server-side unified brain: COMPLETE + deployed + validated (2026-06-14)** (`POST /api/diagnose-turn` — see Diagnostic Engine Architecture). **SB4 — the mobile switch onto `/api/diagnose-turn`: COMPLETE + validated on a real F-350 (2026-06-14).** The `/diagnose` thread now runs on the ONE unified brain — `runDiagnoseTurn` (one call/turn) + `dispatchTurn` (renders the discriminated `question`/`assessment`/`diagnosis` onto the EXISTING message + assessment-card tracks) + `buildTurnHistory` (`lib/turnHistory.ts`, PURE + node-gated — serializes prior chat + assessment + captured-evidence turns into the conversation history sent back; this was deferred risk #6). The parallel `/api/assess` + `/api/diagnose` double-fire is gone (those endpoints stay on the server but are unused by the thread; the local `runAssessment`/`callApi` are dead-marked for a focused cleanup). A DATA_CAPTURE turn drives the existing 2C-4 capture executor; the captured evidence is interpreted by the reused `/api/evidence-update` (Tier-1 decision). **This makes the unified `/api/diagnose-turn` THE live diagnostic path.** **Three real-device defects found on the truck and fixed (DB-1/2/3), all re-validated on-vehicle (2026-06-14):** DB-1 re-run crash (terminal-user guarantee in `buildTurnHistory`), DB-2 capture-won't-start (app-wide catalog persistence on connect + a `startCaptureRound` network fallback), DB-3 guided-by-absence (connected+empty-snapshot → REQUEST A CAPTURE, see/hear/feel guard verbatim). **The unified-flow merge arc SB1–SB4 is COMPLETE and hardware-validated.** **Tier-2 (later):** fold `spec_lookup`/`dtc_lookup` into the turn via a generalized `askToolLoop` (mid-turn live retrieval; deferred from SB3). **Deferred polish (NOT regressions):** baseline-poll-on-connect — would let empty-snapshot cases capture on turn-1 instead of asking one setup question first (see Auto-reconnect deferred); and the NHTSA offline-resilience gap, which surfaced live during SB4 truck testing (see Known Gaps → Offline resilience).
- **Ask Vulcan accuracy hardening:** Ask Vulcan now runs **Opus 4.6** (was Sonnet — Sonnet fabricated free-form mechanical facts). A factory-spec provenance rule and an internal-consistency rule live in the shared `APP_CONTEXT`, so all three Claude modes inherit them; for conversational modes the spec rule is **label-not-suppress** (lead with the likely value + a verify note) while Smart Diagnose's `ASSESS_SYSTEM_PROMPT` keeps its stricter no-ballpark variant. The Ask Vulcan response cache now **excludes spec-shaped questions**, is **version+model-keyed**, and **self-prunes** stale entries on load. See Tech Stack (AI) and Backend (response caching).
- **Ask Vulcan spec intent via tool use (PR-1):** `/api/ask` routes spec questions through a `spec_lookup` tool inside an agentic execute-then-continue loop (`server/askToolLoop.js`), so Claude understands phrasings the old `detectSpecIntent` regex missed ("oil change specs", "what oil does it take") and reasons over verified DB rows in the same turn. The regex is demoted to a zero-Claude latency fast-path. `SPEC_CAUTION_PREAMBLE` is retired — the hedge now lives in the tool-miss result text + APP_CONTEXT. Verified end-to-end against the deployed server (regex-missed phrasings state verified F-150 values; misses + no-vehicle hedge; fast-path unchanged; cost sums across loop calls). Diagnose/Assess untouched. This also establishes the tool-loop pattern Stage 2 needs. See Backend (vehicle spec retrieval).
- **Supabase/Postgres data layer — live spec path (PAUSED at a known-good state for Stage 2):** backend connects to Supabase via `pg` over the transaction pooler (`server/db.js`, reads `SUPABASE_DB_URL`); schema via numbered SQL migrations (`npm run migrate`) — `source` / `vehicle_variant` / `spec` / `component_fact` / `spec_miss`, provenance FK on every fact. **The extraction engine IS built (Batch A productionized + validated on Sierra/Ford/Honda, ~$2.2/manual) and the spec read-path is wired into the app under Option C.** Deliberately deferred (none blocks Stage 2): the real extraction feed at scale (near-launch), NHTSA-canonical normalization + structured input (pre-launch), before-feed fixes (page-remap, Honda), and Batch B/C. See Backend → Database layer and `VULCAN_DATA_LAYER_STRATEGY.md` (§11 deferred-work ledger). The existing JSON caches remain untouched (their migration is the separate, still-deferred piece).

- **Component-identity hedging (2026-06-12):** the APP_CONTEXT factory-spec rule now covers vehicle-specific component identity facts (filter type, part numbers, component locations as they apply to a specific year/make/model) under the same label-not-suppress discipline — verified on the deployed server (Subaru filter questions hedge; F-150 verified-DB fast path and numeric-miss hedging unchanged, no timidity regression). Component-shaped questions (`/\bfilter\b/`, `/\bpart (number|#)s?\b/` via `COMPONENT_SHAPED_PATTERNS` in `vehicleSpecs.js`) are excluded from the response cache, and when they get no DB-backed component facts they log demand to `spec_miss` as `spec_type='componentFact'` (fire-and-forget, fail-soft — no `SPEC_TYPE_ENUM` or provider change). `CACHE_VERSION` bumped to v3. **Flagged for approval, not built:** widening the `spec_lookup` tool description so filter questions invoke the oil lookup (making extracted component facts reachable — see the deferred component tool path).

- **Stage 2A mode restructure (2026-06) — ONE merged diagnostic mode:** Diagnose and Smart Diagnose are now a single mode at `/diagnose`; `app/smart-diagnose.tsx` is deleted. Key decisions and mechanics:
  - **Single CTA ("Start Diagnosis"), auto-assessment.** Intake validation is one rule everywhere: mileage + complaint required. Whether a structured assessment runs is the app's decision, not the user's: if the adapter is connected AND (live signals > 0 OR DTCs > 0) — the old Smart Diagnose no-data block rule, repurposed as the firing gate — `/api/assess` fires automatically on start, **in parallel** with the first `/api/diagnose` call. The two calls are independent; an assessment failure shows a compact error card and the conversation proceeds. Connected-but-empty sessions never burn the Opus assessment call.
  - **Thread model (`app/diagnose.tsx`):** the conversation FlatList renders `ThreadRow` items — chat messages interleaved with `AssessmentEntry` cards. Each assessment is anchored by `afterMessageIndex` with a stable key from the moment its loading card renders, so a card resolving after later turns never shifts layout. A "Re-run assessment" link under the latest card (connected + gate passing only) appends a new anchored card.
  - **OBD2 escalation (`lib/obd2Handoff.ts`):** the OBD2 screen's three old buttons ("Diagnose with Vulcan" + "Smart Diagnose" ×2) are now ONE "Escalate to Diagnosis" door → `setObd2DiagnoseHandoff({selectedDescriptors, dtcs, pendingDtcs, permanentDtcs, freezeFrame})` → `router.push("/diagnose")` (push, not replace — OBD2 scan state survives backing out). The in-memory store has split semantics: `getObd2DiagnoseHandoff()` is a non-consuming read feeding the auto-assess gate/badges/snapshot; `consumeObd2DiagnoseEscalation()` is consume-once and feeds the complaint prefill (stored + permanent code lines — deliberately NOT pending codes, to keep text parity with the old handoff and avoid new AI-visible information). Deliberately not persisted (stale-after-restart data must not arm the assessment path). `clearObd2DiagnoseHandoffCodes()` mirrors a code clear into the store (zeroes dtcs/pendingDtcs/freezeFrame, **preserves permanentDtcs** — they survive a Mode 04 clear by definition) so an assessment can never reason over cleared codes. The AsyncStorage `lib/handoff.ts` channel now serves Ask ↔ Diagnose only.
  - **Connection-aware reset:** `resetSession()` in `/diagnose` preserves the vehicle/VIN while an adapter is connected (clearing would desync the global context from the physically-connected vehicle); disconnected, it clears everything as before.
  - **Assessment UI extracted to `components/assessment/`** (`AssessmentResult`, `ConditionSelector`, `DataBadge`) — reused by the merged screen, ready for Stage 2's evidence-update cards. `Obd2Manager.getSelectedPids()` getter added so the home-tile entry path can build snapshots from the live polling selection.
  - **Capture-card placeholder (`components/assessment/CaptureCard.tsx`):** presentational-only Stage 2 primitive (waiting / capturing / complete, progress bar, signal chips, optional cancel). Rendered with mocked tap-to-cycle states at the bottom of every assessment result behind the `DEBUG_UI` flag (see Debug Logging) so it's reviewable on the shop preview build. Stage 2's capture executor will drive it with real state; no component changes needed.
  - **Inspection Report: KEEP (decided 2026-06-10).** Zero cost in the new nav; revisit at UI-redesign time.

- **Stage 2B — diagnostic case save/resume (2026-06, "the patient chart"):** a tech can pause a diagnosis, switch cars, and resume later. Mobile-only; **no server/endpoint/schema changes** (resume re-sends saved conversation history to `/api/diagnose` exactly as an in-session continuation does). This is the persistence layer Stage 2C's evidence loop will write into, so the data model is the load-bearing part. See the Diagnostic Engine Architecture → "Diagnostic case save/resume" subsection for the full design. Key points:
  - **Versioned case envelope** (`lib/diagnosticCasesCore.ts`, `DiagnosticCaseV1`, `schemaVersion`) with reserved-but-empty 2C slots (`evidenceLedger` shaped on `RequestedDataItem[]`+`DiagnosticSnapshot`, `caseState` mirroring `Hypothesis`) so 2C extends rather than retrofits. The envelope carries NO live-connection data by type (no `PidDescriptor[]`/DTC arrays/`FreezeFrame`/snapshot) — that absence IS the history-vs-live staleness guarantee.
  - **Auto-save, no save button** — persists once per completed turn (intake submit, user turn, assistant turn, assessment resolve, confirm/reject) through a serialized write queue. Per-case storage keys + a lightweight index (per-device AsyncStorage).
  - **Keying:** each case has its own generated id (primary key); VIN is an index, not the key (one VIN → many cases; no-VIN cases live in the manual list only). Resume-by-VIN = open cases on that VIN.
  - **Different-vehicle guard (the safety core):** a case resumes AS LIVE only with no connection, or when the connected VIN matches the case VIN; resuming a VIN case while connected to a different vehicle is BLOCKED, and a resumed no-VIN case never enables the live-assessment path. No state ever exists where Claude could reason over the wrong car's live data. Ground-truth VIN comes from `obd2.getConnectedVin()`, never the overridable VehicleContext.
  - **Lifecycle:** open/closed; confirm-fix closes + links the `DiagnosticRecord` (the confirmed-fix DB feed; surfaced as a distinct FIXED chip); cap 25, prune oldest CLOSED first, all-25-open requires explicit consent (delete oldest open, or continue unsaved). Reopen of a closed case is deferred to 2C.

**Open action items (near-term):**

- **iOS native cleanup:** exclude `react-native-bluetooth-classic` pod from iOS build via Expo config plugin (latent crash risk; requires full iOS rebuild; do before TestFlight)
- **Offline resilience:** graceful handling when connectivity drops during connect/VIN-decode (currently fails silently into a degraded PID list); plus offline data buffering for road tests
- **Diagnostic engine Stage 2C:** iterative evidence loop — Claude requests specific data under specific conditions, phone captures automatically via monitoring loop, sends back for an evidence-update call. **The UI home is ready (Stage 2A)** (merged `/diagnose` thread, anchored assessment slots, capture-card primitive with mocked states) **and the persistence layer is ready (Stage 2B)** (the case envelope's reserved `evidenceLedger`/`caseState` slots are shaped for exactly this — 2C populates them). **Stage 2C-1 (monitoring-plan schema) is COMPLETE and deployed (2026-06-13):** the `emit_diagnostic_assessment` tool's `requested_data` was enriched (additively, no fork) so a DATA_CAPTURE next step carries an executable `capture_plan` — a numeric `context_gate` (the "when"), a `measured_target` (the "what" + threshold band), `sustained_seconds`, and `capture_window_seconds`, all in raw OBDb units. `ASSESS_SYSTEM_PROMPT` gained a MONITORING PLAN section + a safety carve-out keeping capture ranges (observation windows) distinct from factory specs (which still route only to `unverified_specs_needed`). A fail-soft soft-validator drops any missing/malformed plan → Stage-1 prose fallback (logs, never throws). The enriched `RequestedDataItem` rides through the 2B `EvidenceCaptureEntry.requested` slot with no retrofit. **Deferred to 2C-2 (flagged): signal-id→command resolution lives on the phone** — validated calls showed Claude normalizes snapshot ids to SAE-canonical ids (emitted `SHRTFT11` when the snapshot said `STFT_B1`, and `SHRTFT11` is itself non-globally-unique — it lives at both `01 14` and `01 15`), so 2C-2's resolver must map Claude's id to the vehicle's supported PIDs tolerantly (prefer the command in the captured/selected set) and report what it can't watch rather than guessing. **Stage 2C-2 (the phone's local capture brain) is COMPLETE (2026-06-13):** a PURE, node-tested core (mirrors `lib/dtcParser.ts`) — `lib/captureResolver.ts` binds Claude's `signal_id`s to the vehicle's signals (3-tier match: exact → normalized → a small flagged SAE alias table; collision rule = prefer-selected → else single-supported → else `ambiguous`, **never guess**; target-unavailable ⇒ item unrunnable, gate-unavailable ⇒ degraded-but-runnable), `lib/captureDetector.ts` runs the noise-tolerant **wall-clock** sustained-hold (dropout tolerance `HOLD_DROPOUT_TOLERANCE_MS`, stale-in-band guard) plus the **four cost safeguards as independent gates** (sustained / per-item cooldown / per-session budget=5 / auto-pause), `lib/captureEvidence.ts` forward-captures the window (the 10s ring buffer is too short for ≤60s windows — forward capture, ring buffer untouched) into an `EvidenceCaptureEntry`. `lib/captureExecutor.ts` is the thin RN glue (subscribes to the new `obd2.onTick`, drives `CaptureCard`, emits the entry). **Design correction made + flagged:** auto-pause is **fresh-read based, not value-change based**, so a steady-state capture (e.g. "sustained warm idle") is not falsely paused while waiting for exactly that condition. **The deferred 2C-1 `lib/assessmentTypes.ts` type shipped in this 2C-2 `eas update`** (the agreed "touches both, bent with eyes open" case — now resolved). 2C-2 makes NO Claude call and does not write a specific case's ledger/render the thread — those are 2C-4. **Stage 2C-3 (the evidence-update endpoint) is COMPLETE and deployed (2026-06-13):** `POST /api/evidence-update` takes the prior assessment + the 2C-2 summarized evidence (`observed` snapshot + `trigger` + `unavailableSignals`) and returns a fresh EVOLVED assessment (same `emit_diagnostic_assessment` schema), reasoning over the new evidence in light of the hypothesis the prior next-step was testing. **Single-shot forced-tool, NOT a tool loop** (the evidence is already captured + summarized — nothing to request mid-turn; `askToolLoop` stays the pattern for 2C-4's auto-continue + PR-2). The shared assessment spine was extracted to `server/assessPrompt.js` (REASONING/MONITORING/SAFETY/FREEZE/OUTPUT sections sliced VERBATIM so the Stage-1 prompt is **byte-for-byte unchanged**, proven by `server/scripts/verifyAssessPrompt.js`), and `runStructuredAssessment` in `server/index.js` is shared by both `/api/assess` and `/api/evidence-update`. Server stays **stateless** — the phone writes the evolved assessment into `caseState` and preserves the prior in history (2C-4). New cost call-type `evidence-update` (bucketed separately in `/api/costs/summary`). Only the IMMEDIATE prior assessment is sent each call (never full loop history → bounded cost as the loop runs deep); the `STRONGLY_SUPPORTED` ceiling holds (no `CONFIRMED` until Stage 3). Validated on the deployed server: a lean/vacuum-leak prior + a captured "trim high & load-independent" window evolved the leak hypothesis to STRONGLY_SUPPORTED and switched the next step to a smoke test; `/api/assess` still returns an identical-shape Stage-1 assessment; and the safety control held (a needed factory MAF value routed to `unverified_specs_needed`, not asserted). `EvidenceUpdateResponse` type added to `lib/assessmentTypes.ts` (compile-time only; committed for contract congruence, OTA rides with 2C-4). **Stage 2C-4 sub-batch 1 (single-round wiring) is COMPLETE (2026-06-13):** the full pipe now runs end-to-end for ONE round — a DATA_CAPTURE assessment card shows a **minimal "Start monitoring"** button (gated to the latest card, like re-run; SUB-BATCH 2 replaces it with the hands-off driving UX) → `startCaptureRound` (`app/diagnose.tsx`) builds the resolve context (cached catalog + bitmask + live selection/unsupported), resolves the plan, **applies the plan signals to the live polling driver** (existing `setSelectedPids`/`startPolling`, in-memory + restored after), and starts a `CaptureExecutor` → the real `CaptureCard` renders in the thread under that assessment (waiting → capturing → complete, driven by the detector via `onCard`) → on fire, `onEvidence` hands the `EvidenceCaptureEntry` **unreshaped** to `evidenceUpdate()` (`lib/api.ts` → `/api/evidence-update`) → the evolved assessment lands as a new anchored card (running → done, reusing the Stage-1 slot pattern). **The load-bearing persistence fix:** `saveCase` was hardcoding `evidenceLedger: []`/`caseState: null` on every write (erasing each round's output); it now persists `evidenceLedgerRef`/`caseStateRef` (REPLACE-current `caseState.hypotheses` = evolved hypotheses; one `stepsTaken` line per round; prior preserved in `assessments[]`), restored on resume, cleared on reset. The DEBUG_UI `CaptureCardDevPreview` mock was removed (the real executor now drives the card). Graceful degradation: cancel (Stop watching), unrunnable target ("can't watch X on this vehicle"), `/api/evidence-update` failure (errored evolved card, evidence still persisted), and disconnect mid-round all end the round cleanly; the executor subscription is torn down on complete/cancel/disconnect/reset/unmount (one round at a time). **SCOPE: one round only — no auto-continue.** If an evolved assessment is itself DATA_CAPTURE its card shows Start again (manual re-press), but nothing auto-executes. What remained for 2C — **sub-batch 2** (self-continuing loop + per-round Start-gated hands-off driving UX; removes the per-session fire-count cap=5 while keeping sustained/cooldown/auto-pause) and **sub-batch 3** (real-car validation) — was **superseded by the unified-flow merge (SB1–SB4):** the self-continuing capture loop is now delivered through the unified `/api/diagnose-turn` turn + the 2C-4 executor + `/api/evidence-update` (an evolved assessment that is itself DATA_CAPTURE re-arms the Start gate), the per-session fire-count cap was removed (sustained/cooldown/auto-pause kept), and the whole loop was **hardware-validated on a 2016 F-350 (2026-06-14)**. Nothing in 2C remains open. See the Unified diagnostic flow merge bullet above and Diagnostic Engine Architecture → the unified diagnostic turn.

**Diagnostic engine Stage 2 / Claude-directed live monitoring — DELIVERED (unified-flow merge, hardware-validated 2026-06-14):**

- Claude's DATA_CAPTURE next-step carries `requested_data` (+ an executable `capture_plan`); the phone auto-executes it INSIDE the unified `/api/diagnose-turn` turn — waits for the condition, captures the window locally (zero API cost), and sends one `/api/evidence-update` call with the captured evidence for an evolved assessment. The self-continuing loop falls out of the unified turn (an evolved assessment that is itself DATA_CAPTURE re-arms the Start gate). Cost safeguards live: sustained-condition requirement, per-PID cooldowns, auto-pause on inactivity (the per-session monitoring budget cap was removed once the loop was hardware-proven). Uses `PidDescriptor.aiSelected` (wired). See the Unified diagnostic flow merge bullet, Diagnostic Engine Architecture → the unified diagnostic turn, and Roadmap → Claude-directed live monitoring (the as-built design).

**Pre-launch work not yet started:**

- **Infrastructure:** migrate JSON-file storage to the Supabase/Postgres data layer (connection + schema + productionized extraction engine + live spec read-path all in place — see Backend → Database layer; remaining: the real extraction feed at scale, then move the existing cache data off the JSON files) (bundle in self-hosted NHTSA vPIC VIN decoder + NHTSA recall/TSB caching); fallback AI provider (Claude → GPT-4o for 529s); real auth (Supabase); billing (RevenueCat + Stripe); Sentry; Mixpanel; API cost monitoring + usage limits; build proprietary spec database; move confirmed-fix database to cloud
- **Product:** UI redesign for premium feel; Inspection Report keep-vs-remove revisit at UI-redesign time (KEEP decided 2026-06-10 during the Stage 2A restructure — zero cost in the current nav, it's the app's only customer-facing PDF deliverable); speech-to-text; compatible-adapters screen
- **Legal/business:** form LLC + EIN; trademark Vulcan (Class 9 & 42, VulcanDX backup); switch Apple/Google accounts to Organization; CPA/attorney consult
- **Launch:** TestFlight + Google Play internal testing; finalize tiered pricing ($40-50/mo target); App Store listing prep; **strip BOTH debug flags (`EXPO_PUBLIC_DEBUG_OBD2`, `EXPO_PUBLIC_DEBUG_UI`) from `.env` before any customer-facing build or OTA update** — local OTA exports inline `.env`, so this is a hard release gate, not tribal knowledge (see Development Workflow → OTA env inlining)

**Phase tracker:**

| Phase | Status |
|---|---|
| 1. Get off Expo Go | COMPLETE |
| 2. OBD2 Foundation | COMPLETE |
| 3a. Diagnostic engine — Stage 1 (single-shot assessment) | COMPLETE |
| 3b. Diagnostic engine — Stage 2 (iterative evidence loop) | COMPLETE & hardware-validated (2016 F-350, 2026-06-14) — Stage 2A UI restructure COMPLETE (merged diagnostic mode, OBD2 escalation handoff, capture-card placeholder); **Stage 2B case save/resume COMPLETE** (versioned envelope, auto-save, VIN-match resume, different-vehicle guard); **Stage 2C-1 monitoring-plan schema COMPLETE + deployed** (executable `capture_plan` in the assessment: context gate + measured target + sustained-hold + window; prompt + soft-validator + safety carve-out); **Stage 2C-2 condition detector + capture executor COMPLETE** (pure resolver + noise-tolerant sustained-hold detector + four cost safeguards + forward-capture → `EvidenceCaptureEntry`; 83-test node gate; shipped via `eas update` to `preview` carrying the deferred 2C-1 type); **Stage 2C-3 evidence-update endpoint COMPLETE + deployed** (`POST /api/evidence-update`: prior assessment + summarized evidence → evolved assessment; shared `assessPrompt.js` spine with a byte-equality guard proving Stage-1 unchanged; `evidence-update` cost type); **Stage 2C-4 sub-batch 1 (single-round wiring) COMPLETE** (Start → captureExecutor → /api/evidence-update → evolved card + chart write; `saveCase` now persists `evidenceLedger`/`caseState`); **Unified-flow merge SB1–SB4 COMPLETE + hardware-validated on a 2016 F-350 (2026-06-14)** — the `/diagnose` thread runs entirely on the unified `/api/diagnose-turn` brain (one move/turn: talk / request-a-capture / conclude), the self-continuing capture loop is delivered via the unified turn + the 2C-4 executor + `/api/evidence-update`, prior turns are serialized back via `buildTurnHistory`, and three on-truck defects (DB-1 re-run crash, DB-2 capture catalog, DB-3 guided-by-absence) were fixed + re-validated. Deferred polish: baseline-poll-on-connect, offline-resilience |
| 3c. Diagnostic engine — Stage 3 (adaptive stance UI, guided checklists) | IN PROGRESS — **Step 1 (guided result-capture) COMPLETE**: a directed physical inspection can carry 2–4 brain-authored `finding_options` rendered as glove-friendly tap buttons; the tapped finding feeds the existing `/api/diagnose-turn` loop. Additive + fail-soft (`softValidateFindingOptions`), open-question-no-buttons stays structural, byte-equality guard untouched, `CONFIRMED` ceiling held. Photo attach-point seam left (not built). Remaining Stage 3: adaptive-stance UI switching, the `CONFIRMED` confirmation gate, guided checklists |
| 4. Pre-launch infrastructure | IN PROGRESS — Supabase data layer: foundation + productionized extraction engine (validated Sierra/Ford/Honda/Subaru) + live Option C spec path + tool-use routing all landed; paused in a known-good state (the Stage 2 engine it supports is now complete). Pending: real extraction feed at scale, NHTSA normalization + pickers (pre-launch), JSON→Postgres migration |
| 5. Testing & launch | NOT STARTED |

## Scalability Requirements

**This is a permanent project requirement, not a one-time note.** Vulcan is being built to support thousands of concurrent users at launch and beyond. Every piece of code written must be evaluated for scalability.

- Whenever you write or modify code, consider whether it will hold up at thousands of concurrent users. If it won't, flag it explicitly and explain what the scalable solution would be, even if we implement a simpler version for now.
- Avoid architectural decisions that would require a complete overhaul to scale. Prefer solutions that can grow.
- **Current known scaling considerations:** JSON-file caching on the Railway Volume works for now but will likely need to migrate to a proper database (Supabase/Postgres) before launch. Flag any new feature that adds to file-based storage load.
- When a current implementation is a temporary simplification, leave a clear code comment marking it as such and noting the scalable replacement.
- Backend API endpoints should be stateless where possible so they can scale horizontally.
- Do not introduce per-user data stored in ways that won't scale (e.g. growing single files).

## Tech Stack

- **Frontend:** Expo SDK 54, React Native, TypeScript
- **Backend:** Node.js/Express on Railway
- **Database:** Supabase (Postgres) is the backend's DB for the new unified vehicle-data layer (the spec database). Connected server-side via `pg` (node-postgres) over the **Supabase Transaction Pooler** (Supavisor, port 6543) — see DB Layer section. Live: connection + schema + a productionized extraction engine + the spec read-path wired into the app (Option C). Deferred: the real extraction feed at scale and the JSON-cache → Postgres migration. **The existing JSON caches are unchanged and remain the source of truth for their features** (`cache.json`, `dtcCache.json`, `vehicleSpecCache.json`, `pidCache.json`).
- **AI:** Claude Opus 4.6 for Diagnose mode AND Ask Vulcan mode (Ask Vulcan switched from Sonnet 4.6 to Opus 4.6 — Sonnet was fabricating free-form mechanical facts, e.g. wrong oil-filter location/type, that no spec rule governs; the model `ASK_MODEL` is the single source of truth at `server/index.js`). Sonnet 4.6 still runs the background DTC-fallback path (`dtcFallback.js`). All three Claude prompts (`ASK_SYSTEM_PROMPT`, `SYSTEM_PROMPT`/Diagnose, `ASSESS_SYSTEM_PROMPT`/Smart Diagnose) begin with a shared `APP_CONTEXT` block (`server/index.js`) so Claude reasons as an integrated diagnostic tool — aware that the app retrieves VIN/DTCs/live data through the Vulcan OBD2 connection, knows the four app modes, and treats codes appearing in conversation as confirmed scans rather than hypothetical. Updates to the shared identity belong in `APP_CONTEXT` so all modes stay consistent. **Factory-spec rule lives in `APP_CONTEXT`** (so all three modes inherit it in one place). For the conversational modes it is **label, not suppress**: Claude reasons freely about diagnosis/procedures/how-systems-work AND may state a commonly-known numeric spec (capacity, torque, viscosity, pressure, sensor voltage/range, RPM target, gap, interval, fill spec) **or a vehicle-specific component identity fact (filter type, part number, component location on a specific year/make/model — widened 2026-06-12 after shop testing caught confident Subaru oil-filter hallucinations that the numeric-only wording never claimed to cover)** — but must lead with it as a *likely value to confirm against the OEM source*, never as a precise authoritative factory figure asserted as gospel. When an exact value was injected as verified data, it's stated directly as confirmed. (An earlier pass made this a hard refusal/provenance rule, which over-hedged and suppressed widely-known specs; relaxed to label-not-suppress.) **Smart Diagnose's `ASSESS_SYSTEM_PROMPT` keeps its own stricter, absolute `NON-NEGOTIABLE` variant** — zero unverified numbers, all routed to the `unverified_specs_needed` schema field, no ballpark allowance. The layering is explicit and safe: APP_CONTEXT carries a deference clause ("if a mode sets a stricter spec rule routing unverified specs to a dedicated field, that stricter rule governs that mode"), so the relaxed conversational baseline does NOT loosen the structured assess path. **`SPEC_CAUTION_PREAMBLE` has been retired.** Ask Vulcan's spec hedge now lives in exactly two places: the `spec_lookup` tool-miss result text and the APP_CONTEXT factory-spec rule (which is always present and governs every spec question regardless of phrasing). The old injected preamble was removed only after a deployed Layer B proof confirmed all three paths it used to cover (tool miss, regex fast-path miss, no-vehicle ask) still hedge via APP_CONTEXT + tool text alone. **Internal-consistency rule also lives in `APP_CONTEXT`** (all modes inherit it): when stating specific mechanical facts tied to data being cited (a part number, spec, DTC, or injected verified data), the answer must square with that data; a self-contradiction (e.g. citing a spin-on filter's part number while calling it "cartridge-type") must be reconciled before answering or flagged as uncertain. **Deliberately scoped to internal consistency / claims tied to cited data — NOT blanket second-guessing**; it must not reintroduce the timidity that was tuned out of the spec answers (those still lead with the useful value). Added after Opus self-contradicted on a 2023 Impreza filter type while citing the correct spin-on OEM part number in the same response.
- **OBD2:** `react-native-ble-plx` for BLE, `react-native-bluetooth-classic` for Classic Bluetooth
- **DTC parsing:** Three modes queried on every scan — Mode 03 (stored), Mode 07 (pending), Mode 0A (permanent/confirmed). See DTC Parsing Architecture section below.
- **Protocol detection:** `ATDPN` queried after the `0100` handshake pass. CAN (codes 6–9): ATH1 stays, frame-aware parser, multi-PID batching. Non-CAN (codes 1–5): ATH0 applied, flat-scan parser, single-PID polling. Protocol name shown in status bar.
- **Builds:** EAS Build for iOS and Android

## App Structure — Four Main Modes

1. **Ask Vulcan** — open-ended automotive conversation, no VIN required
2. **Diagnose** — THE diagnostic mode (Stage 2A merged the former standalone Smart Diagnose into it; the unified-flow merge then put the whole thread on the `/api/diagnose-turn` brain). Intake (VIN/manual + mileage + complaint) → a SINGLE diagnostic brain that, each turn, decides whether to ask/explain, request a live capture, or conclude. When a vehicle is connected it reasons from a live snapshot and can request an automatic capture (the tech just taps Start + operates the vehicle); questions, assessment cards, and the final diagnosis all render in one thread (see Diagnostic Engine Architecture → Unified diagnostic turn)
3. **Inspection Report** — multi-point vehicle inspection with PDF export (KEEP decided 2026-06-10; revisit at UI redesign). Self-contained: own local vehicle state (not on `VehicleContext`), own VIN decode, no Claude/OBD2 dependencies
4. **OBD2 Scan** — Bluetooth connection to vehicle, DTC reading, live data. A simple instrument with ONE door into diagnosis: "Escalate to Diagnosis" sets the `lib/obd2Handoff.ts` store and pushes `/diagnose`

## Key Architecture Details

- **Hybrid retrieval system** — DTC database lookup, vehicle spec providers, and response caching on backend before calling Claude API to reduce costs and prevent hallucinated values
- **Dual Bluetooth transport** — BLE for iOS, Classic for Android, unified abstraction layer in `lib/obd2.ts`
- **Auto-reconnect** for saved OBD2 adapters — adapter identity (id, name, transport, last-connected timestamp) is persisted to AsyncStorage in `lib/savedAdapter.ts` after a successful handshake. Only one adapter is remembered at a time. **APP-LEVEL since unified-flow SB2 (2026-06-14):** connection is established app-wide, not trapped in the OBD2 Scan screen — `Obd2Provider` (app root) calls the single gated `obd2.ensureAutoReconnect()` on launch + on `AppState`→active, so any screen (Diagnose especially) sees a connected vehicle without first visiting the OBD2 screen; Diagnose also retries it on focus. `ensureAutoReconnect()` is a no-op when already connected/connecting, when there's no remembered adapter, or when permissions aren't already granted (`hasRequiredPermissions()` — a NON-prompting check, so first-time pairing stays a deliberate OBD2-screen action with no cold-launch Bluetooth prompt). **Concurrency:** all connects route through a mutex (`runExclusiveConnect`) — concurrent attempts to the same device coalesce, different targets serialize, so the proactive `teardownAfterDisconnect()` can never double-fire (the prerequisite that makes app-wide triggering safe). The OBD2 screen's own mount reconnect is retained (mutex-safe; provides on-open retry + the manual picker fallback on failure). The connected VIN is exposed reactively via `Obd2Context.connectedVin` (manager `onVin` signal, set on Mode-09 parse / cleared on disconnect — it resolves AFTER status "connected", which a status-only watcher would miss) — the clean "connected + what is it" signal the unified-flow merge reads. **Alert-suppression truth table** (`VehicleContext`, `show = !silentReconnect || genuineChange`): user picker connect → show; user connect to a different vehicle → show; silent reconnect to the SAME vehicle → suppress; silent reconnect to a DIFFERENT truck → show; silent reconnect with no prior vehicle loaded (launch race) → suppress. `wasLastConnectSilent()` distinguishes a silent `connectDirect` from a user `connect`; the same-VIN early return in the auto-VIN handler covers the hydrated same-truck case, the truth table covers the launch race. **Diagnose connect affordance (SB2-D):** when no adapter is connected, the Diagnose intake shows a "Connect an OBD2 adapter" button routing to the OBD2 picker (`router.push("/obd2")` — no duplicate picker), and "Connecting…" while in flight. **The original VIN-import symptom is resolved as a consequence** — SB1 (read the VIN from `obd2.getConnectedVin()` ground truth) was correct; the remaining gap was simply that nothing TRIGGERED a connect outside the OBD2 screen, so on a Diagnose-first open `getConnectedVin()` was null. App-level triggering closes it. **Validated on a real vehicle (2016 F-350, 2026-06-14):** connect-from-Diagnose with no OBD2 visit + VIN import, silent remembered-reconnect on launch (no popup, no permission prompt), genuine vehicle-change still alerts, failure→picker, and the launch+open-OBD2 double-connect produced one clean connection (mutex holds on hardware) with a correct status bar and stable vehicle-switch. **DEFERRED (merge-time follow-up, NOT a regression):** passive PID polling when connected app-wide without an OBD2-screen visit — today the ring buffer only fills once the OBD2 screen starts polling a tech-chosen PID selection, so a pure Diagnose-first connect has `getConnectedVin()`/`isConnected` but no live snapshot. This is unchanged from before (today's Stage-1 passive auto-assess also needs the OBD2 screen), the 2C capture flow self-starts polling, and the merge's reach-for-monitoring DECISION signal is `isConnected`+`connectedVin` (not the snapshot) — so it's a polish item (baseline-poll-on-connect / PID defaults) for the merge, deliberately out of SB2 scope. **Update (DB-2, 2026-06-14):** the CATALOG half of this was lifted app-wide — `VehicleContext` now `fetchPidCatalog`+`saveCatalog` on every vehicle change (which fires on connect/VIN-decode), so the capture flow's `loadCachedCatalog` resolves with no screen visit (+ a `startCaptureRound` network fallback for the cold-cache race). What remains deferred is the passive POLLING itself (a baseline live snapshot on connect); until it lands, a Diagnose-first connected start has an empty snapshot and the unified brain asks one operating-condition question before its first capture (DB-3), reaching a capture by turn-2
- **Auto-VIN + global vehicle context** (`contexts/VehicleContext.tsx`) — when the OBD2 adapter handshakes successfully, `Obd2Manager.getVin()` issues Mode 09 PID 02 and parses the multi-frame ISO-TP response (`parseVinFromResponse` in `lib/obd2.ts` tokenizes hex bytes, skips CAN IDs and ISO-TP markers, walks the bytes after the `49 02 01` header and keeps only valid VIN characters). The 17-char VIN feeds `decodeVin()` (NHTSA) which populates the global vehicle. Ask Vulcan, Diagnose, and the OBD2 screen all read from this context, so the vehicle bar reflects the connected vehicle everywhere. If the tech had manually entered a different vehicle before connecting, an alert offers a choice between the two. Vehicles older than ~2008 may not expose Mode 09 PID 02 — `getVin()` returns null and the tech falls back to manual entry silently. The context persists the current vehicle + VIN + source (`"manual" | "vin-decoded" | "obd2-auto"`) to AsyncStorage so the same vehicle is restored on app launch. On vehicle change, the context backgrounds-fetches recalls/TSBs and prefetches `/api/pids/:make/:model/:year` so the live-diagnostic feature has warm cache. **Scaling:** all vehicle state is per-user and client-side (AsyncStorage); no backend per-user storage was added
- **TSB and recall integration** via NHTSA API
- **Diagnostic hierarchy** — visual inspection first, simple checks before advanced tests
- **Confirmed fix recording system** for building proprietary diagnostic database

## Backend

- Hosted on Railway, auto-deploys from GitHub `main` branch

### Database layer (Supabase / Postgres) — live spec path

The unified vehicle-data layer's storage. **Live, not foundation-only anymore:** connection + schema + a productionized extraction engine (validated cross-manufacturer) + the spec read-path wired into the app under Option C. **Still deferred:** migrating the *existing* JSON caches (`cache.json`/`dtcCache.json`/`vehicleSpecCache.json`/`pidCache.json`) into Postgres (those remain the source of truth for their features), and the real extraction feed at scale. See `VULCAN_DATA_LAYER_STRATEGY.md` for the full plan, reasoning, and the deferred-work ledger (§11).

- **Connection (`server/db.js`)** — uses `pg` (node-postgres), NOT `@supabase/supabase-js`: we need server-side DB access only (no auth/realtime) and the project's **Data API (PostgREST) is disabled**, so the HTTP client can't reach tables. `pg` opens a direct Postgres connection over the **Supabase Transaction Pooler** (Supavisor, port 6543). Connection string comes ONLY from `process.env.SUPABASE_DB_URL` (set in `server/.env` locally and the Railway dashboard) — **never hardcoded, never logged/printed**. A single bounded `pg.Pool` (`max` via `PG_POOL_MAX`, default 10) sits in front of the pooler so multiple Railway instances don't exhaust it; `ssl: { rejectUnauthorized: false }` (Supabase requires TLS).
- **Transaction-pooler discipline** — Supavisor transaction mode assigns a different upstream connection per transaction, so per-connection session state is NOT stable. Use ONLY parameterized (unnamed) queries; no named prepared statements, no session-scoped `SET`/`LISTEN`/`NOTIFY`. node-postgres' default parameterized queries are unnamed, so no special flag is needed (unlike Prisma's `pgbouncer=true`).
- **RLS** — RLS is enabled on the project and governs the (disabled) Data API's anon/authenticated roles. The pooler connection authenticates as the database-owner role, which **owns the tables and bypasses RLS** (no `FORCE ROW LEVEL SECURITY`), so server-side reads/writes work with RLS on. Migrations run as that role so ownership holds. Do NOT disable RLS to fix a server-side access issue — investigate role/ownership instead.
- **Fail-loud / no silent degrade** — a MISSING `SUPABASE_DB_URL` is fatal at startup (`process.exit(1)`, same pattern as `ANTHROPIC_API_KEY`). If the URL is present but Supabase is unreachable, the startup probe (`initDb()`, a `SELECT 1` fired from `app.listen`) logs a loud `[db] ERROR` and sets `isDbReady()` false but does NOT crash. The spec path is now a **live consumer** (`lookupSpec` in Ask Vulcan/Diagnose/Assess), but it is **fail-soft** — `lookupSpec` can never throw and a DB-down degrades to a clean miss → honest fallback, so a Supabase blip still must not take down OBD2/Claude. `isDbReady()` gates the per-call readiness check; look for `[db] connected to Supabase` in the deploy logs to confirm live connectivity.
- **Schema migrations** — version-controlled SQL in `server/migrations/NNNN_*.sql`, applied by a minimal runner (`npm run migrate` → `server/migrate.js`) that records applied files in a `schema_migrations` table and runs each pending file in a transaction. Manual/deliberate invocation only — NOT run on server boot (auto-migrating would race across Railway instances). Run once against the shared Supabase DB. Tables (`0001_init.sql`): `source` (provenance), `vehicle_variant` (8-column granular UNIQUE on mechanical config), `spec` (typed values, `confidence`, NOT NULL `source_id`), `component_fact` (non-numeric facts, NOT NULL `source_id`), `spec_miss` (miss log). Controlled-vocab columns use CHECK constraints with a starter set — widen them in a later migration. Every `spec`/`component_fact` carries a `source_id` FK so provenance rides on every fact.
- **Smoke test** — `npm run db:smoke` (`server/scripts/dbSmokeTest.js`) inserts a `source` row, reads it back, deletes it, confirms it's gone. Proves end-to-end read/write through the pooler. PASS/FAIL only, no secrets.
- **Extraction engine (`npm run extract:pdf`, `server/scripts/extractFromPdf.js`)** — doc → verified rows: a source PDF → `source` row → Claude (opus-4-6, forced `tool_choice` on `emit_extracted_specs`) extracts specs + component facts → validation gate → `spec`/`component_fact` with the `source_id` FK → read-back. **Core rule:** extract ONLY what the doc states; every item needs a `verbatim_quote` + page (no quote → quarantined, never stored). Manual-run only, not wired into any endpoint; does not touch the live spec read-path or JSON caches (extraction is the sole *writer* of specs; the app only reads). **Status: PRODUCTIONIZED (Batch A) and validated cross-manufacturer (Sierra + Ford + Honda + Subaru).** The 2023 Impreza run (2026-06-12, official STIS PDF, $1.25/run) is the first real demand-driven feed entry: 48 specs + 21 component facts live under source_id=8, verified end-to-end through `/api/ask` (regex fast-path card AND tool path). It surfaced two new manufacturer-shaped assumptions, both fixed: a provenance-based `--identity-override` (Subaru manuals carry no model/year in extractable text) and `normUnitForMatch()` (Subaru "US qt"/"lbf·ft" spellings quarantined all ten capacity/torque specs on the first run). `scripts/trimPreflight.js` (zero-cost) now exists to check trim coverage before spending. **Data-source finding: owner's manuals do NOT state oil-filter part numbers/locations** — filter-identity demand (visible in `spec_miss` as `componentFact`) needs a parts-catalog or tech-verified source. See `VULCAN_DATA_LAYER_STRATEGY.md` §6.1 (Subaru run record, findings #16/#17). Batch A added trim-before-extract (local pdfjs-dist keyword-density page scan feeds only spec-bearing bands), a widened `spec_type` vocab (+ an `other`-needs-a-label rule), and NOT NULL `page` + `verbatim_quote` audit columns. **Cost: ~$2.2 per large manual** (594-page Sierra, ~66% cut from the $6.31 full-manual baseline via trimming), a one-time build-once-per-vehicle spend — **NOT the originally-assumed sub-$1** (that assumed specs lived in ~50–80 pages; they actually span ~170 across the back half). A `--full` baseline diff proved the trim drops **zero core specs**. Large docs exceed Opus's 1M context, so the script chunks (no silent truncation). Parameterized per-run via `--year/--make/--model/--title/--publisher/--url` with a pre-spend `verifyIdentity` PDF-vs-declared guard. **Full plan, the cost correction, the cross-manufacturer findings (GM-shaped-assumptions lesson, page-remap + Honda before-feed fixes, vocab/unit widening), and the deferred Batch B/C work all live in `VULCAN_DATA_LAYER_STRATEGY.md` §6.1 + §11 — not duplicated here.** The test PDFs live under the gitignored `server/extraction_test/`. **Still deferred:** the real extraction feed at scale (demand-ranked off the `spec_miss` log, near-launch).
- Railway URL baked into EAS preview and production builds via `eas.json` env block
- DTC database in `server/dtcData.json` (~1.1 MB, sourced from the MIT-licensed [Wal33D/dtc-database](https://github.com/Wal33D/dtc-database) SQLite distribution and converted to JSON at integration time) — 18,805 entries across 33 manufacturers plus 9,415 generic SAE J2012 codes. Loaded into memory at server startup by `server/dtcDatabase.js`. JSON shape: `{ [code]: { [MANUFACTURER]: description } }`. We dropped the SQLite/`better-sqlite3` path because Railway's Nixpacks builder has no Python for `node-gyp` to compile native bindings — JSON keeps the deploy native-dep-free
- DTC endpoint `GET /api/dtc/:code` accepts optional `?make=Ford&engineType=3.5L 6-cyl Turbocharged` — `make` prefers a manufacturer-specific definition; `engineType` feeds the config-mismatch detector (see below). Falls back to the generic SAE entry, then to pattern handlers (cylinder/coil-specific copy), then to a Claude fallback (`server/dtcFallback.js`) that produces a structured definition via tool use (Sonnet 4.6) and persists it to `server/dtcCache.json` so each missed code only hits Claude once. Cache is keyed by `code:make`, gitignored, in-flight requests for the same key are deduped, and Claude failures are NOT cached
- **Diagnose mode runs the same DTC enrichment server-side** (`server/index.js` `/api/diagnose`) — extracts codes from the presenting complaint, looks each one up via `lookupDtc(code, vehicle.make)`, attaches any config-mismatch flag, and injects the verified definitions into Claude's system context via `formatDtcContextBlock`. This means manufacturer-aware lookups and mismatch warnings benefit Diagnose mode too, not just the OBD2 screen and Ask Vulcan
- **Config-mismatch detector** in `server/dtcMismatch.js` — extensible rules engine that flags codes whose described system doesn't match the decoded vehicle. Current rules: forced-induction codes on non-FI engines (turbocharger/supercharger/wastegate/boost references on engineType lacking turbo/ecoboost/supercharged/etc.) and diesel-specific subsystem codes on gas engines. Add new rules by appending a `{id, matchEntry, matchVehicle, message, severity}` object to the `RULES` array. The flag rides on the DTC response as `configMismatch: {id, message, severity}`, rendered in the OBD2 DTC card as a small warning banner and injected into the Diagnose system context so Claude treats the code with appropriate skepticism. The VIN decoder (`lib/api.ts:decodeVin`) surfaces NHTSA's `Turbo`, `FuelTypePrimary`, and `OtherEngineInfo` fields into the `engineType` string so the keyword-match rules can fire
- The source database has no causes/urgency/system metadata, so `system` is derived from the code type letter (P/B/C/U), `urgency` defaults to `medium`, and `commonCauses` is `[]` — except where a pattern handler fires (those still produce rich copy)
- **Response caching for Ask Vulcan factual queries** (`server/cache.js`, 30-day TTL on the Volume). Two guards added after the 2026-06 stale-cache incident:
  - **Spec-shaped questions are NEVER cached.** They must be generated live and spec-guarded on every call, because a cached spec answer survives prompt/model changes and freezes a possibly-stale figure for 30 days. The `/api/ask` cache-eligibility gate excludes anything `isSpecShapedQuestion(text)` matches (`vehicleSpecs.js`) — a single gate that controls both the read and the write, so a spec question is neither served from nor written to the cache. **`isSpecShapedQuestion` is a deliberately broad, load-bearing detector** — strictly wider than `detectSpecIntent`/`SPEC_PATTERNS` (the narrow guardrail-injection detector). The whole incident was "oil change specs" slipping `detectSpecIntent` but matching the old cacheable-question heuristic, so it cached a pre-fix answer. For the cache-exclusion decision we err toward NOT caching: a false positive costs one uncached answer (cheap); a false negative freezes a stale, unguarded spec (the bug). When widening spec coverage, update `isSpecShapedQuestion`, not just `SPEC_PATTERNS`.
  - **The cache key is version-keyed.** `buildCacheKey(vehicle, question, model)` stamps the key with a manual `CACHE_VERSION` constant **and** the model name: `CACHE_VERSION::model::vehicle::question`. Bump `CACHE_VERSION` by hand whenever an Ask Vulcan prompt change should invalidate old answers; a model swap auto-invalidates without a bump. Old-regime entries become cache MISSES and regenerate under the current model+prompt instead of being served stale. (This is why the unversioned `v1` entries from before the fix stopped being served the instant `v2` deployed — their keys no longer match.) On load, `cache.js` also **prunes any entry lacking the current `CACHE_VERSION` prefix** — so a version bump not only orphans old entries (unreachable) but reclaims their disk on the next deploy. No admin endpoint or shell access is needed to clear stale answers; bumping `CACHE_VERSION` is the lever.
- Vehicle spec retrieval in `server/vehicleSpecs.js` — `detectSpecIntent`/`SPEC_PATTERNS` pattern-detect factual spec questions (oil capacity, torque, fluids, battery, maintenance intervals), and `lookupSpec(vehicle, specType)` routes through a provider chain. Provider modules live in `server/specProviders/` and follow a `{ id, configured(), lookup(vehicle, specType, params, fetcher) }` shape. **The sole spec source is now the Supabase vehicle-data layer (`supabaseSpecs.js`). Vehicle Finder was removed — proven a dead end (3 spec types, sparse, silently-wrong records).** The provider shape is kept so a future Tier-2 retrieval-grounded provider can slot in.
  - **Ask Vulcan (`/api/ask`) routes spec intent via TOOL USE, not the regex.** The brittle `detectSpecIntent` adjacency regex missed phrasings like "oil change specs" / "what oil does it take" (it caused the 2026-06 hedged-answer-despite-correct-data bug on a 2020 F-150). Spec routing now lives in `server/askToolLoop.js`: an agentic execute-then-continue tool loop registers a `spec_lookup` tool (input = `spec_types` enum only; the vehicle is injected server-side from request context, not a tool param), Claude calls it on any spec question — including indirect phrasings — and reasons over the verified rows in the same turn. **Hybrid seam:** `detectSpecIntent` is kept ONLY as a zero-Claude latency fast-path (regex fires + structured vehicle + DB hit → formatted card, no Claude call); every other case (no regex match, regex-hit + DB-miss, or no vehicle) flows to the tool loop. **Both doors call the identical `lookupSpec` — one source of truth.** The loop has an env-configurable iteration cap (`ASK_TOOL_MAX_ITERATIONS`, default 3) plus a forced text-only final call so it can't hang, sums cost across every call into one `ApiCostData` object, and reports `toolInvoked` so a tool-using response is never cached (live data). Adding a future tool (e.g. a Batch C fuse-diagram lookup) is "register one entry" in `ASK_TOOLS` + `ASK_TOOL_HANDLERS` — no loop rewrite. **This loop is deliberately the same execute-then-continue machinery Stage 2's evidence loop needs** (Claude requests specific data under specific conditions → server provides → Claude continues in the same turn); `askToolLoop.js` is the reusable socket both future retrieval tools and Stage 2 plug into, not Ask-Vulcan-only plumbing. The live `/api/ask` spec path's plumbing is **proven end-to-end** (PR-1's deployed testing: in-DB 2020 F-150 returns DB rows on regex-missed phrasings; misses + no-vehicle hedge; fast-path unchanged; cost sums across loop calls). **Diagnose/Assess do NOT use the tool loop** — they keep their forced-tool calls and proactive `detectAllSpecIntents` spec-context injection. **PR-2 (folding Diagnose/Assess onto this tool-loop pattern) is deliberately deferred until AFTER Stage 2**, because Stage 2's evidence loop will shape what a unified Diagnose tool-loop should look like (data-capture requests + spec lookups through one loop). Decided to wait, not skip — see `VULCAN_DATA_LAYER_STRATEGY.md` §11.
  - `supabaseSpecs.js` — read-only provider into the unified Supabase spec DB (built in the Batch A extraction work; tables `spec`/`component_fact`/`vehicle_variant`/`source`). **Full Option C is live on the spec path.** A lookup resolves the vehicle to `vehicle_variant` rows on `(year, make, model)` + **salvaged engine-token matching** (the granular-key lesson carried over from Vehicle Finder's `combineModelSeries`, but adapted: the DB stores `model` bare — "Sierra" — with the truck class in each spec's `qualifier`, so series is a **row-level label/disambiguator, NEVER folded into the model**). When a `spec_type` has several rows differing by class/engine, **all matching rows are rendered, each labeled** (e.g. "16 L (1500 Series)" vs "15.5 L (2500/3500)") rather than guessing one — the Vehicle-Finder wrong-record substitution is structurally impossible; an ambiguous/no-match is a clean miss, never a wrong record. Cross-source precedence is **single-best-source** (highest `trust_tier`, then newest `created_at`) with **§6.C conflict logging** (a losing source whose value disagrees is logged for review — the general mechanism that will later arbitrate a manufacturer doc vs. a retrieved web page, not a test-data hack). Relevant component facts are folded in (oil → oil-filter part number) for both the Ask-Vulcan answer and the Diagnose/Assess context block. **On a miss OR a DB outage, returns null → an honest hedged answer** (the two are indistinguishable from the user's side, differing only in logging): in Ask Vulcan the `spec_lookup` tool-miss result text + the APP_CONTEXT spec rule carry the hedge; in Diagnose/Assess no spec block is injected and the prompt's spec rule governs. The miss is written to the `spec_miss` demand queue (best-effort, fail-soft, guarded against malformed input). **STRICT STORE:** this path only READS `spec`/`component_fact` and only WRITES `spec_miss`; extraction is the sole writer of specs.
  - `openLabor.js` — Open Labor Project. **Scaffolded, disabled, and no longer routed** (left on disk, not in the provider chain). Revisit only if a fluids/labor source is wanted later.
- **Fail-soft is the load-bearing property here:** `lookupSpec` is wrapped so it can **never throw** — a DB-down or any query error degrades to a clean miss (honest fallback), so the spec DB is an enhancement, never a hard dependency, and a Supabase blip cannot 500 the Diagnose/Assess paths (which `await lookupSpec` OUTSIDE their endpoint's inner try/catch). **DB hits are NOT written to `vehicleSpecCache.json`** — the DB *is* the persistent store, so double-caching would re-introduce staleness (a re-extraction wouldn't surface) for no benefit (DB reads are cheap, no external API cost/latency). The old JSON spec cache is now vestigial (still loaded on boot, ignored for the DB path); the hit/miss/error counters remain for `/metrics` and the startup rollup.
- **Deferred (NOT done in this wiring pass):** Batch B fuzzy/normalized dedup — near-dup hit lines (e.g. "Oil type: dexos" + "Oil type: dexos specification") and the text-phrasing conflict-log noise on the identical test sources (both log/cosmetic, absent in production where a vehicle has one source). And **coverage-widening** for the `spec_type`s Batch A added but neither `detectSpecIntent` nor the `spec_lookup` tool enum can yet request (fuel/axle/transfer-case/gvwr/gawr/idle/spark-gap/refrigerant/tire). **Narrowed (no longer "unguarded") hole:** "spark plug gap" / "refrigerant" questions still get no DB lookup (not in the tool's `spec_types` enum, not in `detectSpecIntent`), so they reach Claude without verified data — but they are NO LONGER unhedged, because the APP_CONTEXT factory-spec rule is always present and hedges every spec question. The widening pass (add these to `SPEC_TYPE_ENUM` + `SPEC_TYPE_MAP` + `SPEC_PATTERNS`) closes the remaining gap by routing them to the DB for a verified answer.
- **Cache storage location** is resolved by `server/cacheDir.js`: reads `CACHE_DIR` env var, defaults to `server/` for local dev. On Railway, a Volume is mounted at `/data` and `CACHE_DIR=/data` is set, so `cache.json`, `dtcCache.json`, and `vehicleSpecCache.json` all live on the Volume and survive redeploys. Without the Volume, the caches reset on every redeploy and previously-answered Claude DTC questions get re-billed. See DEV_SETUP.md for the Railway dashboard walkthrough. A `[startup] cache rollup:` log line at server start shows the loaded entry counts per cache — if those reset to zero on every deploy, the Volume isn't wired correctly
- When a spec question goes to Claude in Ask Vulcan (a DB miss OR a DB outage — the two behave identically here), the hedge comes from the `spec_lookup` tool-miss result text ("no verified record… give a likely value to confirm against the OEM source, never as a confirmed factory spec") plus the always-present APP_CONTEXT factory-spec rule. **This covers the no-structured-vehicle path too:** with no year/make/model, `spec_lookup` returns its explicit no-vehicle result and Claude asks for the vehicle or hedges via APP_CONTEXT — it does not state a number as fact. The no-vehicle fall-through is still counted (`vehicleSpecs.noVehicleFallthroughs`, surfaced at `/metrics`) so the true Claude-spec-answer rate isn't a blind spot. A regex fast-path hit with a vehicle still returns the verified card directly with no Claude call. *(All three of these hedge paths — tool miss, regex fast-path miss, no-vehicle ask — were verified against the deployed server before `SPEC_CAUTION_PREAMBLE` was retired.)*
- Diagnose mode scans the presenting complaint for spec mentions and proactively injects any verified specs as a system context block so Claude reasons against real data instead of recollection
- **Selectable live PID monitoring** (`app/obd2.tsx` + `app/obd2-pids.tsx`) — the OBD2 screen's live data view polls only the signals the technician has selected. Selection is keyed by OBDb **signal id** (e.g. `RPM`, `MIL`, `DTC_CNT`), not command code, so signals sharing a command response (MIL + DTC_CNT + 22 readiness bits all live at `01 01`) can be selected and decoded independently. The selection screen at `/obd2-pids` groups signals by category (Engine / Fuel System / Air-Intake / Oxygen Sensors / Emissions / Speed-Transmission / Electrical / Other) — categories are assigned server-side in `pidDatabase.js:categorizeSignal`. Status signals (bit-level / enum) get a `STATUS` badge in the picker. Saved presets and per-vehicle selected/unsupported sets live in AsyncStorage under `vulcan:pids:*:v2:<vehicleKey>`. **Scaling:** all preference storage is per-device, no backend per-user state added
- **PID polling driver** (`lib/obd2.ts`) — `startPolling(selectedPids, options)` drives the tick loop. Selected signals are grouped by command code; each unique command is sent ONCE per cycle and every selected signal at that code is decoded from the shared response using its own bit-range (`startBit`, `length`). Mode 01 commands batched up to 6 per request via ELM327 multi-PID syntax. Mode 22 manufacturer commands poll sequentially every Nth tick. **Critical detail:** `LiveValues` is keyed by signal id and updated immutably (`{...liveData, [id]: {...}}`) — in-place mutation would short-circuit React's `prevState === nextState` check and gauges would never re-render. Single-command fallback fires automatically when a multi-PID batch returns partial results (common on GM ECUs that only answer the first PID). Per-signal consecutive-miss counter promotes to `unsupportedPids` only after `MAX_CONSECUTIVE_MISSES`. All `sendCommand` IO is serialized via a Promise mutex inside `Obd2Manager` so bitmask queries / VIN reads / poll commands can't race on the shared CommandBuffer. `getSupportedMode01Pids()` walks PID 00/20/40/60/80/A0/C0/E0 support bitmasks for selection-UI filtering
- **`decodePidGeneric` byte-slicing fix:** When `startBit=null` and `length=N` (byte-aligned signal), the function slices the input to `ceil(N/8)` bytes before computing `raw`. Without this, a command returning multiple signals (e.g. PID `0x14` returns [O2 voltage, STFT B1S1]) would fold all bytes into `raw` before applying the divisor, producing ~115V instead of ~0.45V. The fix is in `lib/obd2.ts:decodePidGeneric`. Signals with `startBit != null` (bit-field extraction path) are unaffected.
- **Live data display** has two sub-sections driven by `isLiveMonitorable` / `isStatusSignal`. Byte-aligned scalar readings (RPM, coolant temp, throttle, fuel trims) render as numeric gauges. Bit-level + enum signals (MIL, readiness flags, DTC count) render in a separate Status panel as colored `MIL: ON` / `Catalyst Ready: Ready` / `Stored DTCs: 3` rows. Both sub-sections update on the same polling loop
- **Units handling** lives in `lib/units.ts`. `formatLiveValue(raw, obdbUnit, {system, signalName, signalId})` and `formatStatusValue(raw, unit, enum)` are pure display-layer conversions that take raw OBDb-unit values (celsius / kPa / km/h / kilometers) and return US-imperial output by default (°F / psi / mph / mi). Internal `LiveValue.value` stays in the raw decoded units so any downstream consumer (the Diagnose-mode snapshot injection, records export) has a single source of truth. `UnitSystem` is `"imperial"` by default — a future user preference reads from AsyncStorage and passes the alternate system into the formatters without any API change. Signal-aware overrides: barometric pressure (BARO / signal name contains "baromet") stays in **kPa** to match scan-tool convention and stay comparable to MAP; EVAP vapor pressure (EVAP_VP / EVAP_VPA / EVAP_VP_WIDE / signal name contains "vapor pressure") converts to **inH₂O** since the values are tiny and US service literature uses inH₂O for EVAP leak diagnosis. MAF stays in g/s in both systems (universal tech convention)
- PID definitions live in `server/pidDatabase.js`:
  - **Standard SAE J1979 PIDs** (294 signals, modes 01-09) bundled at deploy time as `server/pidStandard.json`, sourced from [OBDb/SAEJ1979](https://github.com/OBDb/SAEJ1979). Returned by `GET /api/pids/standard`
  - **Vehicle-specific PIDs** lazy-fetched from `https://raw.githubusercontent.com/OBDb/<Make>-<Model>/main/signalsets/v3/default.json` on first request per vehicle, cached forever in `pidCache.json` on the Volume. Returned by `GET /api/pids/:make/:model/:year` merged with the standard set and filtered by year via OBDb's `filter.from`/`filter.to` per command. Vehicles OBDb doesn't cover return the standard-only response (source: `"standard-only"`) — caller still gets the SAE baseline
  - Each PID exposes: `command: {mode, pid}`, `code`, `id`, `name`, `description`, `path`, `unit`, `min`, `max`, `suggestedMetric`, and a `decode: {length, multiplier, divisor, offset, signed, startBit, enum}` block for raw-byte interpretation
  - OBDb is CC-BY-SA-4.0; attribution lives in the repo-root `NOTICE` and every API response carries `source`/`license` fields

## Debug Logging

Mobile uses bundle-time-inlined debug flags in `lib/debug.ts`. High-frequency
logs (per-poll-tick, transport TX/RX, BLE scan ingestion, per-handshake
commands) are wrapped in `if (DEBUG_OBD2)` and stay silent unless
`EXPO_PUBLIC_DEBUG_OBD2=1` is set in `.env`.
Rare/informational events (connection PASS/FAIL, marked-unsupported,
disconnect, errors, the duplicate-signalKey assertion warning) always log.

**How the flags actually reach builds (corrected 2026-06):** EAS *builds*
take env from `eas.json`'s `env` block, which never sets debug flags — so
full binaries are clean. But `eas update` runs `expo export` **locally**,
which inlines `EXPO_PUBLIC_*` from `.env` — so OTA bundles carry whatever
`.env` had at export time. `EXPO_PUBLIC_DEBUG_OBD2=1` is currently in
`.env` **intentionally** (shop-test phase wants verbose OBD2 logging on the
preview build). Stripping BOTH debug flags from `.env` before any
customer-facing build/update is a tracked pre-launch gate (see Pre-launch →
Launch).

Second flag: `DEBUG_UI` (`EXPO_PUBLIC_DEBUG_UI=1`, also intentionally in
`.env`) gates placeholder / in-progress UI — currently the Stage 2
capture-card primitive with mocked tap-to-cycle states at the bottom of
every assessment result. Remove from `.env` once the real Stage 2 capture
flow drives the card.

## Development Workflow

- Use **Windows CMD** (not PowerShell) for all terminal commands
- Development builds connect to local Expo dev server via `npx expo start --dev-client`
- Preview builds are standalone, built with `eas build --profile preview --platform ios/android`
- OTA updates for JS-only changes: `eas update --channel preview --message "description"`. **The shop/testing build runs on the `preview` channel** — JS-only fixes are shipped there so the standalone preview build on the test devices picks them up. (The `development` channel pairs with the dev-client build connected to the local Metro server; use it only when iterating against `npx expo start --dev-client`.)
- **OTA env inlining (load-bearing):** `eas update` runs `expo export` locally and inlines `EXPO_PUBLIC_*` vars **from `.env`** into the bundle; `eas.json`'s `env` block applies to EAS Build servers ONLY (full binaries). Consequence: whatever debug flags are in `.env` at export time ship in the OTA. Currently intentional (`EXPO_PUBLIC_DEBUG_OBD2=1` + `EXPO_PUBLIC_DEBUG_UI=1` for the shop-test phase); stripping both is a pre-launch release gate.
- Native module changes require full EAS rebuild
- Push to GitHub for Railway backend deploys
- The **Railway CLI is authenticated** for this project (linked to service `vulcan-backend`). Use it to verify deploys went live and inspect logs without the dashboard: `railway status`, `railway logs --service vulcan-backend` (add `--build` for build logs, `--deployment` for runtime/startup logs). This is how to confirm a redeploy is live and read startup lines like `[startup] cache rollup`, `[cache] loaded … pruned …`, and `[db] connected to Supabase`. (`railway ssh` needs an SSH key, which is not configured — prefer log inspection.)

**Deploying changes — Claude Code's responsibility:** When a task involves backend/server changes, Claude Code must commit and push to GitHub `main` as the final step so Railway redeploys — this is not the user's job to do manually. Backend changes do NOT reach the live server via OTA updates (`eas update` only ships the mobile JS bundle to devices; server code only deploys when pushed to GitHub). A task that changes both mobile and server code requires BOTH an `eas update` (for mobile) AND a `git push` (for server) — these are always separate steps and both must be completed. Never consider a task with server changes finished until the push to GitHub is done. Always confirm to the user that both the OTA update and the GitHub push were completed.

## Known Platform Issues

### iOS startup crash — reanimated v4 / worklets babel plugin (RESOLVED)

For a window in development, the app crashed on iOS ~200ms after launch (Swift `_assertionFailure(_:_:file:line:flags:)` from `EXC_BREAKPOINT / brk 1`) before any UI appeared. Initial suspect was `react-native-bluetooth-classic` under the New Architecture; the crash log ruled that out (its iOS native code is Objective-C, not Swift). Actual cause: `babel.config.js` referenced `"react-native-reanimated/plugin"` — the v3-style name — while the project is on reanimated 4.1.1 + `react-native-worklets` 0.5.1, which **moved the babel plugin into the worklets package**. The correct name is `"react-native-worklets/plugin"`. With the wrong plugin loaded, worklet callbacks (`useSharedValue`, `useAnimatedStyle`, `runOnUI`, etc.) were never transformed at build time, and the reanimated runtime asserted on first attempt to execute one on the UI thread. Android tolerated the same misconfig because the Android worklets runtime has softer assertions than iOS Swift preconditions. **Don't regress this — if you upgrade reanimated/worklets in the future, keep the babel plugin name in sync with whichever package owns it in that version.**

### iOS Classic Bluetooth — pending native cleanup

Apple's MFi restriction blocks Bluetooth Classic on iOS for non-licensed peripherals, so `react-native-bluetooth-classic` cannot function on iOS at all. The app already routes platform-dependent: **iOS → BLE only, Android → BLE + Classic** (see `Obd2Manager.connect()` and `connectDirect()`). The JS-side import is platform-gated via a lazy `require()` in `lib/obd2.ts` (only loads on Android), and `BluetoothDevice` comes in as a type-only import so iOS bundles never reference the runtime module.

**Open action item before TestFlight:** the Classic library's iOS Pod still autolinks into the binary on iOS even though the JS layer never touches it. It's dead weight on iOS plus a latent native-init crash risk if a future RN/Expo update breaks the library's iOS shim. Fix is an **Expo config plugin** that excludes the pod from the iOS build (`react-native-bluetooth-classic` removed from the iOS Podfile via the config plugin). Requires a full `eas build --profile development --platform ios` afterward, not OTA. The JS-side lazy-require gate is in place as partial defense for now.

## Hardware Compatibility

| Adapter | Android | iOS | Notes |
|---|---|---|---|
| **OBDLink MX+** | ✓ (Classic) | ✗ | MFi-locked. Used on the ONN Android test tablet |
| **OBDLink LX** | ✓ (Classic) | ✗ | Same MFi restriction as MX+ |
| **Veepeak OBDCheck BLE+** | ✓ (BLE) | ✓ (BLE) | Confirmed working on iPhone test device |

**For end-user recommendations:** Vulcan should prefer BLE-capable adapters in marketing/onboarding copy. Classic Bluetooth adapters work on Android only — be explicit about this when guiding users to a purchase.

## Known Gaps

### Pre-2008 / no-Mode-09 diagnostic log attribution

Vehicles that don't support Mode 09 PID 02 return `null` from `obd2.getVin()`, so no raw VIN is ever available. `updateSessionVehicle()` is never called, and the diagnostic session stays attributed to whatever vehicle was in context at connect time (typically the previous session's vehicle). This is **expected behavior, not a bug** — there is no hardware identifier to key on without Mode 09. For clean shop-test data on these vehicles, the tech should manually set the vehicle in context before connecting.

This limitation is related to the offline-resilience gap, but the decode-failure case behaves correctly: when Mode 09 *does* return a VIN but the NHTSA decode fails (no network), the new VIN is detected and — because it differs from the vehicle currently in context — the stale year/make/model are **cleared** rather than carried forward. The session (and the global vehicle context) attributes to the bare VIN with a blank name, rendering as just the VIN. It does **not** mislabel the session with the previous car's name. The VIN is always the reliable source of truth for which physical vehicle a session belongs to; the name is best-effort and may be blank until decode succeeds or the tech fills it in manually (which re-stamps the session). What can't be recovered is the case where Mode 09 returns null entirely — no VIN, no hardware identifier to key on.

### Offline resilience  ⚠️ PRIORITY — surfaced live during SB4 truck testing (2026-06-14)

**This gap bit during real-vehicle SB4 validation** (a VIN-decode failure on the F-350), so it is bumped from "near-term" to a priority pre-deeper-feature item. The two-part fix below is unchanged; the planned **self-hosted NHTSA vPIC decoder** (Pre-launch infrastructure) removes the network dependency on the NHTSA API entirely and is the durable fix.

The connect-and-decode-VIN sequence fails ungracefully when connectivity drops mid-flow (e.g. WiFi-to-cell handoff while in a shop): `decodeVin()` (NHTSA API) silently returns null, the global vehicle stays empty/partial, and the PID list falls back to the SAE-only "standard-only" path with no error surface to the technician. Adapter connection itself succeeds — the symptom is a thin / degraded experience downstream of the failed network call.

**Planned fix has two parts:**

1. **Graceful VIN-decode-failure handling** — when NHTSA returns null or times out, keep the raw VIN string, prompt the tech to retry once connectivity returns, or offer manual vehicle entry. Don't silently degrade.
2. **Offline data buffering** — local OBD2 collection continues working without network (everything except backend lookups already does); buffer any DTCs / live snapshots / Claude-ready context locally, sync when reconnected. This pairs with the Claude-directed monitoring roadmap item below since that feature relies on local capture by design.

### Parked bugs (pre-existing, known, unchanged — kept visible so they're not lost)

These predate the recent data-layer/tool-use work and are unaffected by it. Recorded here so they stay on the radar; none is currently being worked.

- **VIN scan inconsistency** — Mode 09 VIN read / decode doesn't always succeed consistently across attempts on the same vehicle/adapter. (Related to but distinct from the pre-2008/no-Mode-09 ceiling above, which is expected behavior; this is intermittent failure on vehicles that *should* return a VIN.)
- **Volvo XC70 — no PIDs** — this vehicle comes back with no usable live-data PIDs (likely an OBDb-coverage / protocol-handling gap for that make/model); falls through to the standard-only path with a thin experience.
- ~~**Stuck "adapter connected" status bar**~~ — **FIXED (2026-06-12, pending shop-device confirmation).** Root cause: the Classic transport's disconnect listener read `event.address` from `react-native-bluetooth-classic`'s `DEVICE_DISCONNECTED` event, but the payload nests it as `event.device.address` — the match never fired, so `teardownAfterDisconnect()` never ran and status stayed "connected" (Android/Classic only; BLE was always correct). This also exposed the Stage 2A auto-assess gate: polling kept refilling the ring buffer with stale `liveData` under the stale status, so an Opus assessment could fire against a dead adapter with stale data framed as a fresh snapshot. Fix in `lib/obd2.ts` (ClassicTransport disconnect handler) reads the nested address with a flat fallback, case-insensitive, still strictly address-matched (the ACL event fires for ANY Bluetooth device). Shipped via OTA to `preview`. Known residual edge (deliberately left): toggling the phone's Bluetooth radio off may not fire BLE's per-device `onDisconnected` on some `ble-plx` versions — would need a `bleManager.onStateChange` watcher if it ever shows up in testing.

### iOS regression pass + Classic-pod cleanup (pre-TestFlight)

Still on the roadmap, slotted after the data-layer pause / before deeper feature work: the pre-TestFlight iOS native cleanup (exclude the `react-native-bluetooth-classic` pod via an Expo config plugin — see Known Platform Issues) plus a general iOS regression pass. Kept visible here; not started.

## Claude-directed live monitoring — DELIVERED (as-built design)

> **Status: DELIVERED** via the unified-flow merge and hardware-validated on a 2016 F-350 (2026-06-14). The design below is the **as-built description**, not an upcoming feature — it runs inside the unified `/api/diagnose-turn` turn (see Diagnostic Engine Architecture → the unified diagnostic turn). The one change from the original plan: the per-session monitoring-budget cap was removed once the loop was hardware-proven (the other three safeguards are live).

Cost-aware deep OBD2 integration that folds in the formerly-deferred "Claude auto-applies PID selections" capability. As-built design:

1. Technician describes the problem in Diagnose mode (e.g. "stalls at idle when warm").
2. Claude analyzes the complaint + vehicle context and emits a structured **monitoring plan** via tool call: a list of PIDs to watch, expected ranges, and trigger conditions (e.g. "STFT1 sustained > +10% for 30s while RPM 600-900").
3. Phone applies the plan to the OBD2 polling driver via `PidDescriptor.aiSelected = true` (data path is already wired through `lib/units.ts`, `lib/pidCatalog.ts`, and the gauge highlight style). Monitoring runs **locally on the device — zero API cost** while the technician drives or operates the vehicle.
4. When a trigger condition fires, the phone packages the captured window of live data + the trigger context and sends ONE Claude call to interpret the event and recommend next steps.

**Cost safeguards (load-bearing — without these the feature is unbounded API spend):**

- **Sustained-condition requirement** — a trigger must hold for N seconds (per-PID, configurable) before firing. Prevents noisy single-frame spikes from triggering Claude.
- **Per-PID cooldown timers** — once a trigger fires, that condition can't refire for M minutes. Prevents oscillating sensors from spamming.
- **Per-session monitoring budget** — *(removed once the loop was hardware-proven; documented here for history)* a hard cap on Claude calls per monitoring session (was 5). The sustained-condition / per-PID cooldown / auto-pause safeguards remain the live bounds.
- **Auto-pause on inactivity** — if no PID values change meaningfully for X minutes (vehicle off, adapter idle), polling pauses and the monitoring session is paused. Resumes on activity.

**Scaling implication:** the LOCAL polling + trigger logic is per-device with zero backend load. The Claude calls when triggers fire ARE backend load but bounded by the safeguards above to dozens of calls per session at most.

## Protocol Detection Architecture

After the `0100` sanity check passes in `runHandshake()`, the handshake issues `ATDPN` to query the protocol the ELM327 locked onto during auto-detection.

### Classification

| Code | Protocol | Family | Handling |
|------|----------|--------|---------|
| 1 | J1850 PWM | Non-CAN | ATH0, single-PID polling |
| 2 | J1850 VPW | Non-CAN | ATH0, single-PID polling |
| 3 | ISO 9141-2 | Non-CAN | ATH0, single-PID polling |
| 4 | KWP2000 (5-baud) | Non-CAN | ATH0, single-PID polling |
| 5 | KWP2000 (fast) | Non-CAN | ATH0, single-PID polling |
| 6–9 | ISO 15765-4 CAN variants | **CAN** | ATH1 unchanged, frame-aware parser, multi-PID batching |
| parse fail | unknown | CAN | Treated as CAN (safe fallback) |

### Key invariants

- **CAN path is completely unchanged.** ATDPN adds one query to the handshake; if the result is CAN, no further action is taken. ATH1, the frame-aware dtcParser pipeline, and multi-PID batching (up to 6) are all untouched.
- **ATH0 for non-CAN.** Strips non-CAN 3-byte headers from responses. `parseDtcResponse` already falls back to flat-scan when no 3-char tokens are found. `extractPidDataBytes` and `parseMultiPidResponse` both tokenize to 2-char hex and work identically with headers off.
- **VIN decode (Mode 09)** works with ATH0 — `parseVinFromResponse` filters to 2-char hex tokens regardless of headers. Older vehicles without Mode 09 support return NO DATA → `getVin()` returns null → silent fallback to manual entry (already handled).
- **Single-PID polling on non-CAN.** Batch size is 1 vs 6 for CAN. Older protocols handle multi-PID requests inconsistently; single-PID avoids the fallback noise during first contact with an unknown vehicle.
- **Protocol in status bar.** The `setStatus("connected", ...)` message includes the detected protocol name: "Connected · ISO 9141", "Connected · CAN", etc.
- **Protocol in diagnostic log.** Session start entries (per `diagnosticLogger.startSession`) include `protocol` and `protocolType` so every shop visit is tagged with the vehicle's protocol.

### Storage

`Obd2Manager.protocolType` and `protocolName` are set during handshake and cleared to "unknown" on disconnect.

## DTC Parsing Architecture

### Modes queried

Every `obd2.scanDtcs()` call queries three modes and returns all three arrays:

| Mode | Echo byte | Returns | Description |
|------|-----------|---------|-------------|
| `03` | `0x43` | `dtcs[]` | Stored / confirmed codes |
| `07` | `0x47` | `pending[]` | Pending (not yet confirmed) |
| `0A` | `0x4A` | `permanent[]` | Permanent — survive code clear, require drive cycle to extinguish |

**B-codes (body codes)** are stored by GM BCMs in proprietary mode ($19), not in Mode 03/07/0A. They are unreachable via generic SAE OBD II. Vulcan cannot read them; professional factory-protocol scan tools (Autel, Snap-on GM mode) can. This is an expected data ceiling, not a parsing bug.

### Multi-ECU CAN response format

Modern CAN vehicles have multiple ECUs (PCM, BCM, EBCM…) all responding to a DTC query simultaneously. With `ATH1` (headers on), the ELM327 concatenates all responses:

```
7E8 02 43 00   ← PCM:  PCI=02 (2 data bytes), mode echo 43, count=00 (0 stored DTCs)
7EB 02 43 00   ← EBCM: same
7EA 02 43 00   ← BCM:  same
```

After the tokenizer drops 3-char CAN IDs, the byte stream is: `02 43 00 02 43 00 02 43 00`

**The parser must bound each decode to its CAN frame** using the ISO-TP PCI byte (always the byte immediately before the mode echo in the filtered stream). For a single-frame response `0x0N`, `N-1` bytes of data follow the mode echo.

### Count-byte detection (GM format)

Some ECUs (confirmed on 2011 GMC Sierra PCM) prepend a count byte `B` before the DTC pairs: `[MODE_ECHO] [COUNT] [PAIR1_A] [PAIR1_B] ...`. The count-byte format is detected when `frameData[0] * 2 === frameData.length - 1`. SAE no-count format (pairs directly after echo until `00 00`) is the fallback.

### Multi-frame responses

For >3 DTCs a vehicle uses ISO-TP multi-frame. The three-pass pipeline in `lib/dtcParser.ts` handles this fully:
- **First Frame (FF)**: PCI `0x10–0x1F`. Total payload length = `((PCI & 0x0F) << 8) | data[1]`. First 6 payload bytes carried in the FF.
- **Consecutive Frames (CF)**: PCI `0x20–0x2F`. Each CF carries 7 more payload bytes. CFs from the same CAN ID are appended to the FF accumulator regardless of interleaving with other ECUs' frames.
- **Flow control**: handled automatically by the ELM327. We never send FC frames in application code.
- Payload is trimmed to the total length declared in the FF after all CFs are collected.

### Module structure

The DTC parsing layer is isolated in `lib/dtcParser.ts` — a standalone pure module with zero React Native / Expo dependencies. This allows it to be imported and run in Node.js directly for testing.

| File | Role |
|---|---|
| `lib/dtcParser.ts` | All parsing logic: `parseCanFrames`, `assemblePayloads`, `decodePayloadDtcs`, `parseDtcResponse`, `runDtcParserSelfTest` |
| `lib/dtcParser.fixtures.ts` | Test fixtures — real Sierra captures + synthetic multi-frame + pathological cases |
| `lib/dtcParser.test.ts` | Node.js runnable test script (46 tests). Run from project root: `npx ts-node --skipProject --compiler-options "{\"module\":\"CommonJS\",\"moduleResolution\":\"node\",\"esModuleInterop\":true}" --transpile-only lib/dtcParser.test.ts` |
| `lib/obd2.ts` | Delegates `parseDtcResponse` to `dtcParser.ts` via a private shim. Runs `runDtcParserSelfTest()` on module load when `DEBUG_OBD2=1`. |

### Multi-frame ISO-TP reassembly (Priority 1 — COMPLETE)

The three-pass pipeline correctly handles interleaved multi-ECU CAN responses:

**Pass 1 — `parseCanFrames`**: Splits on 3-char hex tokens (CAN IDs as delimiters). Each ECU's frames are collected separately regardless of arrival order. Returns `null` for non-CAN / ATH0 responses (fallback to flat scan).

**Pass 2 — `assemblePayloads`**: Per CAN ID: SF → payload direct; FF → starts an accumulator; CF → appends to accumulator. After all frames processed, trims each multi-frame payload to the total length declared in the FF. Correctly handles `7E8 FF, 7EB SF, 7E8 CF1, 7EA SF, 7E8 CF2` interleaving as confirmed in the real Sierra live-data log.

**Pass 3 — `decodePayloadDtcs`**: Decodes GM count-byte format or SAE no-count format from the clean, reassembled payload. No framing bytes can contaminate the DTC pairs.

### Debug assertions (DEBUG_OBD2=1)

The parser (prefix `[dtc-parser]`) emits `WARN:` or `SUSPICIOUS:` messages when:
- Non-CAN / ATH0 path taken (no 3-char CAN ID tokens found)
- SF frame declares a length but the frame is shorter than declared
- FF assembled fewer bytes than the declared total (missing CFs)
- Orphan CF arrived with no preceding FF from that CAN ID
- Count-byte claimed N codes but fewer bytes were available
- Unconsumed non-null bytes after no-count decode stop
- Decoded code count exceeds 20 (implausibly high — indicates format mismatch)
- Unknown PCI type encountered

Keep `EXPO_PUBLIC_DEBUG_OBD2=1` set during first-run testing on any new vehicle make/model. The self-test also runs at app startup and logs `[dtc-test] ALL N PASSED` — if it fails, do not proceed to vehicle testing.

## Diagnostic Engine Architecture

The diagnostic engine is a local-state + LLM-reasoning hybrid. Core principles (all settled and implemented):

- **Never stream raw sensor data to Claude.** The phone summarizes live data into objective facts; Claude reasons on the summary. This is the central cost-control decision.
- **Phone produces facts, Claude produces judgment.** The snapshot builder (`lib/diagnosticSnapshot.ts`) outputs averaged values and ranges — it never infers "lean condition" or "misfiring." That's Claude's job.
- **Structured output via tool use.** The `/api/assess` endpoint forces Claude to call `emit_diagnostic_assessment` with a strict schema. No prompt-and-pray JSON.
- **Verified data injected before Claude.** Same pipeline as `/api/diagnose`: DTC enrichment, config-mismatch detection, spec injection, recall/TSB blocks — all server-side before the Claude call.
- **Confidence ladder without CONFIRMED.** Stage 1 schema allows POSSIBLE / LIKELY / STRONGLY_SUPPORTED only. CONFIRMED re-added in Stage 3 when a real confirmation gate exists. **(2026-06-15) The final-diagnosis card heading `components/Results.tsx` was renamed `CONFIRMED DIAGNOSIS` → `DIAGNOSIS`.** That heading was a **legacy pre-engine label** (the file `Results.tsx` predates the diagnostic engine — added 2026-05-17 in `146bb21`; the confidence ladder arrived 2026-05-31 in `8f02e58`) and was **never tied to the confidence enum** (`provide_diagnosis`/`FinalDiagnosis` carries no confidence field). It overclaimed against the engine's honest `STRONGLY_SUPPORTED` ceiling, so it was renamed to match. The same `Results` component renders the conclusion in both the live chat (`MessageRow` → `<Results>`) and the saved records detail (`app/records.tsx`), so the one rename fixes both surfaces. This did NOT touch the confidence enum, any schema, prompt, or the byte-equality guard.

### Key files

| File | Role |
|---|---|
| `lib/assessmentTypes.ts` | All types: `OperatingCondition`, `DiagnosticSnapshot`, `DiagnosticAssessment`, `Hypothesis`, `NextStep`, etc. |
| `lib/diagnosticSnapshot.ts` | Snapshot builder — averages ring buffer entries per signal, records absent signals, packages freeze frame. Objective facts only. |
| `lib/obd2.ts` (Obd2Manager) | Rolling 10-second ring buffer maintained after each poll tick. `captureSnapshot(durationMs)`, `getRingBufferAge()`, and `getSelectedPids()` (added in Stage 2A for the home-tile entry path). Cleared on disconnect. |
| `server/index.js` `/api/assess` | Stage 1 assessment endpoint. Since 2C-3 it's a thin wrapper over the shared `runStructuredAssessment` (passes `extraContextBlocks:[]`, `callType:"assessment"`) — external behavior byte-for-byte unchanged. Enriches DTCs from snapshot.dtcs, injects specs, formats snapshot block, calls Opus with `emit_diagnostic_assessment`. |
| `server/index.js` `/api/evidence-update` | **Stage 2C-3 endpoint.** Takes `priorAssessment` + the 2C-2 `evidence` (`observed` snapshot + `trigger` + `unavailableSignals`); `formatPriorAssessmentBlock` + `formatEvidenceBlock` become extra context blocks; calls `runStructuredAssessment` with `EVIDENCE_UPDATE_SYSTEM_PROMPT` + `callType:"evidence-update"`; returns an evolved assessment. Single-shot, stateless. |
| `server/index.js` `runStructuredAssessment` | Shared assessment runner (2C-3): calls `buildAssessmentContextBlocks` then the forced `emit_diagnostic_assessment` call + cost log + `softValidateAssessmentPlan`. Used by both assess + evidence-update. |
| `server/index.js` `buildAssessmentContextBlocks` | **SB3-1:** the shared verified-data system-block assembly (cached prompt + recall/TSB + DTC enrichment + fail-soft spec injection + extra blocks + snapshot block LAST + summary log). Used by `runStructuredAssessment` AND `/api/diagnose-turn`. Generalized: optional `snapshot` (omitted when disconnected) + `extraDtcCodes` (complaint-text codes); `/api/assess` passes a snapshot + no extra codes so its context is byte-identical. |
| `server/index.js` `/api/diagnose-turn` | **SB3 — the UNIFIED diagnostic brain.** One forced-tool call (`tool_choice:"any"`) over the three REUSED tool schemas — `ask_followup_question` / `emit_diagnostic_assessment` / `provide_diagnosis` — so the brain commits ONE move/turn: ask, request-a-live-capture (an `emit_diagnostic_assessment` with `next_step.type:"DATA_CAPTURE"` + `capture_plan`), direct a bounded physical inspection (a `PHYSICAL_INSPECTION` next step + `finding_options` outcome buttons — Stage 3 Step 1, soft-validated by `softValidateFindingOptions`), or conclude. Returns a discriminated `turn` (`{kind:"question"|"assessment"|"diagnosis"}`). Reuses `buildAssessmentContextBlocks` (snapshot when connected; complaint-text DTCs when not) + `buildMessages` (conversation). Cost call-type `diagnose-turn`. **Spec discipline follows the tool** (strict `unverified_specs_needed` on the assessment turn; relaxed label-not-suppress on the conversational tools — via the per-tool scoping note + the APP_CONTEXT deference clause). Prompt = `UNIFIED_BODY` (`assessPrompt.js`). **The phone wiring landed in SB4 — this is now THE live diagnostic path** (`/api/assess` + `/api/diagnose` stay on the server, unused by the thread). |
| `server/assessPrompt.js` `UNIFIED_BODY` | **SB3-2:** the unified-turn prompt — `UNIFIED_HEAD` (three moves + WHAT-YOU-HAVE + the talk/capture/conclude DECISION RULE, with the zero-effort-auto-capture framing making monitoring first-line for connected live-data cases) + reused `REASONING`/`MONITORING`/`SAFETY`/`FREEZE` spine sections (so `ASSESS_BODY` stays byte-identical — guard passes) + `UNIFIED_SPEC_SCOPING` (per-tool discipline) + `UNIFIED_OUTPUT` (pick exactly one tool). **Stage 3 Step 1 added `UNIFIED_INSPECTION_SECTION`** (UNIFIED-only — bounded inspection ⇒ `PHYSICAL_INSPECTION` + `finding_options` buttons vs. open question ⇒ `ask_followup_question`; force-fitting options onto an open question is an error; couldn't-check ⇒ propose an alternative) — composed into `UNIFIED_BODY` only, so `ASSESS_BODY` stays byte-identical and the guard still passes. The numeric-vs-physical guard (MONITORING) keeps monitoring from being indiscriminate. **DB-3 (the connected+empty-snapshot branch, safety-gated + validated on the deployed server):** a CONNECTED vehicle with an empty/absent snapshot still has a full capture path — the brain REQUESTS A CAPTURE to acquire the data (do not downgrade to a physical inspection, and do not ask the tech to read/estimate a numeric value the app can watch) rather than defaulting to GUIDED-by-absence. The SEE/HEAR/FEEL/SMELL guard is verbatim (a brake-grinding complaint still goes physical); qualitative/historical questions remain a valid ASK. Empty-snapshot cases reach a capture by turn-2 (after one operating-condition question); baseline-poll-on-connect (deferred) would make that turn-1. |
| `server/assessPrompt.js` | **Stage 2C-3 shared prompt spine** (side-effect-free). Section constants (REASONING/MONITORING/SAFETY/FREEZE/OUTPUT) sliced VERBATIM from the pre-2C-3 prompt; `ASSESS_BODY` + `EVIDENCE_UPDATE_BODY` + `buildSystemPrompt`. Single source for the strict spec-safety rule. Stage-1 byte-equality enforced by `server/scripts/verifyAssessPrompt.js` (+ `assessBody.snapshot.txt`). |
| `app/diagnose.tsx` | THE diagnostic mode. Phase machine `intake → chat`; the chat FlatList renders `ThreadRow` items (messages + `AssessmentEntry` assessment cards anchored by `afterMessageIndex` with stable keys). **Unified-flow (SB4): the thread runs on ONE call/turn — `runDiagnoseTurn()` (serializes history via `buildTurnHistory`, sends a live snapshot + `connected` only when `captureConnectionOk = isConnected && liveVehicleMatchesCase()`, posts `/api/diagnose-turn`) + `dispatchTurn()` (a `question`/`diagnosis` turn → an assistant `ChatMessage`; an `assessment` turn → a done `AssessmentEntry`, anchored, with `completedAt`; also REPLACE-writes `caseState.hypotheses`).** The old parallel `/api/assess` + `/api/diagnose` double-fire is removed; `onSubmitIntake`/`sendUserMessage`/`onRerunAssessment` all call `runDiagnoseTurn`; `runAssessment`/`callApi` are DEAD-marked (cleanup pass). Logs `assessment` + `diagnose_turn` (`callType:"diagnose-turn"`). **2C-4 capture loop (unchanged + reused):** `startCaptureRound`/`handleCaptureEvidence`/`runEvidenceUpdate`/`cancelCaptureRound` (resolve plan → apply to polling → `CaptureExecutor` → `/api/evidence-update` → evolved card); gated by `captureConnectionOk` (DB-3: a brain-requested capture selects its own PIDs, so no pre-selection requirement). **DB-2:** `startCaptureRound` falls back to `fetchPidCatalog`+`saveCatalog` on a cache miss. `evidenceLedgerRef`/`caseStateRef` persisted by `saveCase`; capture entries stamp `afterMessageIndex` for history ordering. |
| `lib/turnHistory.ts` | **Unified-flow (SB4) PURE** — `buildTurnHistory(messages, assessments, captures)` serializes the thread into the compact, strictly-alternating `ChatMessage[]` the unified brain sees: chat verbatim (assistant `AssistantTurn` JSON → plain text; diagnosis → prose), assessments → `[Assessment]` lead+confidence+next-move, captured evidence → a USER turn summarizing the averaged `observed` snapshot + trigger (never raw). Ordered by `afterMessageIndex` + timestamp; `coalesce` merges adjacent same-role; **`endOnUserTurn` trims trailing assistant turns so the history ALWAYS ends on a user turn (DB-1 — re-run can't end on an assistant assessment; `messages[0]` is the complaint so it can't empty).** The `diagnoseTurn()` client (`lib/api.ts`) truncates it (first + last N). |
| `lib/turnHistory.test.ts` | **Unified-flow (SB4 / DB-1) node gate** (76 assertions): every `runDiagnoseTurn` entry path proven terminal-user — A `onSubmitIntake`, B `sendUserMessage`, C1 re-run fresh-ending-on-assessment (the crash), C2 re-run resumed-ending-on-assessment, C3 re-run after a capture, + resumed-mid-sequence, coalesce, diagnosis, conversational. Same `npx ts-node …` invocation as `dtcParser.test.ts`. |
| `lib/obd2Handoff.ts` | In-memory OBD2→diagnose handoff. `setObd2DiagnoseHandoff()` (escalate door), `getObd2DiagnoseHandoff()` (non-consuming; gate/badges/snapshot), `consumeObd2DiagnoseEscalation()` (consume-once; complaint prefill), `clearObd2DiagnoseHandoffCodes()` (code-clear mirror, preserves permanentDtcs). Deliberately not persisted. |
| `components/assessment/AssessmentResult.tsx` | Structured result display (stance banner, leading hypothesis, next step + requested-data box, differential, ceiling note, unverified specs). Optional `onReset`. Hosts the `DEBUG_UI`-gated capture-card dev preview. |
| `components/assessment/ConditionSelector.tsx`, `DataBadge.tsx` | Intake pieces: operating-condition chip grid; data-available pills. |
| `components/assessment/CaptureCard.tsx` | Stage 2 capture primitive — presentational only (`state: waiting/capturing/complete`, `conditionLabel`, `signalIds`, `durationSeconds?`, `progress?`, `onCancel?`). The 2C-2 executor drives it via its `onCard` callback; no logic inside. |
| `components/assessment/FindingCard.tsx` | **Stage 3 Step 1** guided result-capture primitive — presentational only (`action`, `outcomes`, `onOutcome`/`onCouldntCheck`/`onFreeText`, `onAttachPhoto?` photo seam). Renders the brain-authored 2–4 outcomes as big glove-friendly tap buttons + an always-present "Couldn't check" + a "Type it instead" free-text escape (the client adds those two; the model never authors them). No logic inside; the host (`AssessmentThreadCard`) wires the callbacks and composes the user-message string, exactly like `CaptureCard`. The reserved `onAttachPhoto?` is the photo attach-point seam (absent today → no affordance renders). |
| `lib/findingOptions.ts` | **Stage 3 Step 1 PURE** — `readFindingOptions(nextStep)` defensively reads/sanitizes the 2–4 `finding_options.outcomes` off a `PHYSICAL_INSPECTION` next step (trims, drops non-strings/empties, requires 2–4 → else null = plain text). `hasFindingOptions` (the render gate predicate) + `formatInspectionResult({outcome|note|couldntCheck})` (the single home of the `Inspection result: …` prefix a tapped finding sends back). Node-tested (`lib/findingOptions.test.ts`, 23 assertions). |
| `server/findingOptions.js` | **Stage 3 Step 1** side-effect-free validator (mirrors `assessPrompt.js`'s testable-module discipline) — `isValidFindingOptions` + `softValidateFindingOptions(assessment)`: drops a missing/malformed/<2/>4/non-string/empty `finding_options` on a `PHYSICAL_INSPECTION` (→ plain inspection), leaves other turn types untouched, **never throws** (mirrors `softValidateAssessmentPlan`). Node gate: `server/scripts/verifyFindingOptions.js` (19 checks). |
| `lib/photoEvidence.ts` | **Photo Evidence Step 1 — MODE-AGNOSTIC shared primitive** (Ask Vulcan reuses it next). PURE + node-safe at import (native modules lazy-`require`d inside the impure functions): `pickAndResize` (picker → resize ≤1568px/1.15MP JPEG+base64), `persistPhoto` (durable copy into `documentDirectory`, fail-soft → source URI), `imageBlockForTurn`/`serializePhotoPlaceholder` (the lean block-vs-placeholder rule), `withoutBase64`. Node gate: `lib/photoEvidence.test.ts` (11). |
| `server/diagnoseMessages.js` | **Photo Evidence Step 1** side-effect-free message assembly — `lastUserIndex` + `buildTurnContent(role, text, image, isFinalUserTurn)`: a standard Opus vision image content block ONLY for the final user image-with-base64 turn, else a `… [photo attached]` placeholder string; never throws on a junk image. Used by `index.js` `buildMessages`. Node gate: `server/scripts/verifyDiagnoseTurnMessages.js` (12). |
| `lib/captureResolver.ts` | **Stage 2C-2 PURE** — binds `capture_plan` `signal_id`s to vehicle signalKeys. 3-tier id match (exact → normalized → flagged SAE alias rewrites), collision rule (prefer-selected → single-supported → ambiguous, never guess), runnable/degraded/unrunnable classification, `collectUnavailable`. Type-only `obd2` imports (node-runnable). |
| `lib/captureDetector.ts` | **Stage 2C-2 PURE** — `CaptureDetector.ingestTick()` → events. Wall-clock sustained-hold + `HOLD_DROPOUT_TOLERANCE_MS` dropout tolerance + `STALE_VALUE_MS` guard; the four safeguards (sustained / `CAPTURE_COOLDOWN_MS` per-item / `SESSION_FIRE_BUDGET` / `INACTIVITY_PAUSE_MS` auto-pause, fresh-read based). No timers/`Date.now()` — every tick carries its time. Per-item independent (no consolidation). All tunables are named constants at the top. |
| `lib/captureEvidence.ts` | **Stage 2C-2 PURE** — `buildEvidenceEntry(window, ctx)` → `EvidenceCaptureEntry` via `buildDiagnosticSnapshot` (now pure). Maps the detector's `CapturedWindow` (raw forward-captured ticks + trigger) into the 2B ledger shape. |
| `lib/captureExecutor.ts` | **Stage 2C-2 RN glue** — subscribes to `obd2.onTick`, feeds the detector, translates events to callbacks (`onCard`/`onEvidence`/`onStatus`), `cancel()`/`stop()`/`getUnrunnable()`. Caller (2C-4) supplies the `ResolveContext`/evidence context and binds the callbacks. |
| `lib/captureDetector.test.ts` | **Stage 2C-2 node gate** (83 assertions): resolver tiers/collision/unavailable, fire-timing, noise-no-fire, dropout tolerance, departure-reset, each safeguard in isolation, steady-state-no-false-pause, window/evidence mapping, cancel. Same `npx ts-node …` invocation as `dtcParser.test.ts`. |
| `app/obd2.tsx` | "Escalate to Diagnosis" buttons (DTC section when codes exist; LIVE DATA section with/without PIDs) → one handler sets the handoff store, `router.push("/diagnose")`. `onClearDtcs` mirrors clears into the store. |
| `lib/diagnosticCasesCore.ts` | **Stage 2B PURE core** (zero RN deps, Node-testable). `DiagnosticCaseV1` envelope + reserved 2C slots, `CASE_SCHEMA_VERSION`, tolerant `migrateCase` (never throws / never yields a bad object), `deriveIndexEntry`, pure `selectPrune`, `reconcileIndex`, `CASE_CAP=25`. |
| `lib/diagnosticCases.ts` | **Stage 2B storage** — per-case body keys + one index key, serialized write queue, injectable `KVBackend` test seam. `loadCase` NEVER deletes an unreadable/future-version body (OTA-rollback survival). `upsert`/`close`/`delete`/`linkRecord`/`pruneForNewCase`/`findOpenCasesByVin`, self-healing index rebuild. |
| `lib/diagnosticCases.test.ts` | **Stage 2B gate** (Node, 70 assertions) — migrator never-throws/never-deletes across malformed/truncated/future(99) fixtures, prune selection, self-heal, VIN lookup, index `closeReason`. Run: same `npx ts-node …` invocation as `dtcParser.test.ts`. |
| `lib/activeDiagnoseSession.ts` | Module flag (`setDiagnoseSessionActive`/`isDiagnoseSessionActive`) the diagnose screen sets on `phase==="chat"` so the VIN auto-prompt won't hijack a mid-thread diagnosis. |
| `components/CaseResumePrompt.tsx` | Root-mounted (in `app/_layout.tsx`, inside the providers), renders null. On a connect VIN commit, offers to resume matching OPEN case(s). Suppression: once-per-connect, obd2-auto VIN only, never during an active chat. Fires after VehicleContext's mismatch alert resolves (no alert stacking). |

### Data flow (the unified diagnostic turn — SB4, the live path)

The Stage-1 parallel `/api/assess` + `/api/diagnose` double-fire is RETIRED. The thread now makes ONE `/api/diagnose-turn` call per turn; the brain commits to one move.

```
ENTRY (unchanged): "Escalate to Diagnosis" on the OBD2 screen (sets handoff,
  prefills complaint) OR open Diagnose from the home tile OR resume a saved case.
App-level connection (SB2): a vehicle may already be connected with no OBD2 visit.
  Catalog persisted on connect (DB-2) so the capture flow can resolve it.
  ↓
Intake: vehicle/VIN + mileage + complaint (required)
  ↓
"Start Diagnosis"  /  each tech message  /  Re-run   →   runDiagnoseTurn()   (ONE call)
  ↓
  history  = buildTurnHistory(messages, doneAssessments, captures)   → ChatMessage[] (ENDS ON USER)
  snapshot = captureConnectionOk ? buildDiagnosticSnapshot(ring, …) : null
  connected= captureConnectionOk  ( = isConnected && liveVehicleMatchesCase, the 2B guard )
  ↓
  POST /api/diagnose-turn { vehicle, vin, mileage, complaint, messages: history, snapshot, connected, recalls, tsbs }
  ↓
  Server: buildAssessmentContextBlocks (snapshot when connected; complaint-text DTCs when not)
          → Claude Opus, tool_choice "any" over the 3 reused tools  →  ONE move
  ↓
  turn = { kind: "question" | "assessment" | "diagnosis" }
  ↓
  dispatchTurn(turn):
    question   → assistant ChatMessage   (chat bubble)
    diagnosis  → assistant ChatMessage   (Results + isFinal)
    assessment → done AssessmentEntry (anchored card, completedAt, caseState.hypotheses);
                 if next_step.type === DATA_CAPTURE the Start gate arms (captureConnectionOk)
  ↓ (DATA_CAPTURE — the self-continuing loop, 2C-4 reused)
  tech taps Start → startCaptureRound → CaptureExecutor (local, zero API cost)
    → on fire → handleCaptureEvidence → POST /api/evidence-update (Tier-1) → evolved AssessmentEntry
    → the captured facts re-enter the NEXT turn's history via buildTurnHistory
  ↓
  saveCase persists messages + assessments + evidenceLedger + caseState (resume-safe)
```

### Stage 2 hook

The `NextStep` type includes `requested_data?: RequestedDataItem[]` (present when `type === "DATA_CAPTURE"`). Stage 1 surfaces this to the tech as text in the next-step card. Stage 2 auto-executes it: phone detects the operating condition, captures the window, sends an evidence-update call. **Stage 2C-1 made `requested_data` executable:** each item now carries an optional `capture_plan` (`lib/assessmentTypes.ts` `CapturePlan` — `context_gate: SignalCondition[]` = the "when", `measured_target: SignalCondition` = the "what" + threshold, `sustained_seconds`, `capture_window_seconds`; ranges are `{min,max,unit}` in raw OBDb units, null-bounded for one-sided thresholds). The tool schema (`ASSESS_TOOL` in `server/index.js`) and `ASSESS_SYSTEM_PROMPT` were extended additively, with a soft-validator (`softValidateAssessmentPlan`) that fail-soft-drops a missing/malformed plan back to the Stage-1 prose. The plain `signal_id`/`operating_condition`/`duration_seconds` fields remain as the human-readable summary of the same plan. **Signal-id→PID/command resolution is intentionally the phone's job (2C-2)** — Claude names signals by OBDb id (and tends to use SAE-canonical ids, which aren't globally unique), so the 2C-2 resolver maps them to the vehicle's supported PIDs and reports what it can't watch. **The UI side is staged:** the merged `/diagnose` thread renders assessment cards in anchored slots (an evidence-update card is just another `AssessmentEntry`), and `components/assessment/CaptureCard.tsx` is the watch/capture status primitive, currently behind `DEBUG_UI` with mocked states. **2C-2 (capture executor) and 2C-3 (evidence-update endpoint) are COMPLETE.** The phone detects + captures locally (`lib/captureDetector.ts`/`captureExecutor.ts`) and `POST /api/evidence-update` interprets the summarized window against the prior assessment and returns an evolved one (`server/index.js` + the shared `server/assessPrompt.js` spine). The evidence-update call is **single-shot forced-tool** (mirrors `/api/assess`), not a tool loop — the evidence is captured + summarized before the call. **2C-4 and the unified-flow merge (SB1–SB4) are now COMPLETE + hardware-validated (2026-06-14):** the phone runs the capture loop end-to-end INSIDE the unified `/api/diagnose-turn` thread — a DATA_CAPTURE turn drives `CaptureExecutor` → `/api/evidence-update` → an evolved assessment whose captured facts re-enter the next turn via `buildTurnHistory`; the self-continuing loop falls out of the unified turn (an evolved assessment can itself be DATA_CAPTURE, arming the Start gate again). Real-car validated on a 2016 F-350 (cold-start → connect → capture → evolve → conclude). The per-session fire-count cap was removed (sustained/cooldown/auto-pause kept). (The Tier-2 mid-turn `askToolLoop` retrieval — `spec_lookup`/`dtc_lookup` folded into the turn — remains deferred.) See Backend → vehicle spec retrieval.

### Guided result-capture — directed physical inspections (Stage 3, Step 1)

The hands-on mirror of the Stage-2 auto-capture loop: when the brain directs the technician to *physically inspect* something whose result is **bounded** (e.g. "check the EVAP purge valve — stuck open / stuck closed / holds vacuum"), the app shows **big glove-friendly tap buttons** for the outcome instead of forcing the tech to type, and the tapped finding feeds the existing `/api/diagnose-turn` loop as the next turn's evidence. **No new endpoint, no new turn machinery** — it reuses the assessment turn + `sendUserMessage`.

**The routing decision (load-bearing — keeps "open question ⇒ no buttons" STRUCTURAL):** a directed bounded inspection routes through the **reasoning lane** — `emit_diagnostic_assessment` with `next_step.type:"PHYSICAL_INSPECTION"` + a `finding_options` block (`{ outcomes: string[] }`, 2–4 entries). An open / qualitative / multi-part question routes through `ask_followup_question` (no options, plain text). So whether buttons appear is decided by *which tool the brain picks*, not a UI guess — `question`-kind turns can never grow buttons. The brain is explicitly told (in a UNIFIED-only prompt section) that it MAY ask an open question with no options, and that **force-fitting options onto an open question is an error** (the failure mode from the earlier generic-yes/no attempt, guarded from both sides).

**Additive + fail-soft, exactly like `capture_plan` (2C-1):**
- **Schema:** `finding_options?: FindingOptions` is an optional field on `NextStep` (`lib/assessmentTypes.ts`) and on the shared `ASSESS_TOOL` `next_step` schema (`server/index.js`). Additive — Stage 1's prompt never mentions it, and the **byte-equality guard is on the prompt (`ASSESS_BODY`), not the tool schema**, so adding the field doesn't trip it.
- **Prompt:** a new `UNIFIED_INSPECTION_SECTION` in `server/assessPrompt.js` (composed into `UNIFIED_BODY` only) carries the routing rule + open-question escape + one-inspection-per-card + the **"couldn't check ⇒ missing evidence, propose an alternative, don't assume a result"** instruction. The shared spine (`REASONING`/`MONITORING`/`SAFETY`/`FREEZE`) is **untouched**, so `verifyAssessPrompt.js` still passes — the SEE/HEAR/FEEL/SMELL guard is referenced, not edited.
- **Server fail-soft:** `softValidateFindingOptions` (`server/findingOptions.js`) drops a malformed block → plain inspection (text + typed reply), never throws. Chained after `softValidateAssessmentPlan` in both `runStructuredAssessment` and `/api/diagnose-turn`.
- **Client:** `FindingCard` renders in `AssessmentThreadCard` when the latest done assessment is a `PHYSICAL_INSPECTION` with well-formed options (`readFindingOptions`), gated **latest-card-only + not-yet-answered** (`afterMessageIndex >= messages.length - 1`) and — deliberately, unlike the capture Start gate — **NOT** gated on `captureConnectionOk` (a physical inspection needs no adapter). A tap → `formatInspectionResult(...)` → `sendUserMessage("Inspection result: …")` → the normal turn loop.

**Persistence is free (no envelope slot, no `schemaVersion` bump):** an *answered* finding is a normal user `ChatMessage` in `messages[]` (auto-saved); a *pending* finding re-derives on resume from the latest `PHYSICAL_INSPECTION` assessment with no following user turn (same way the capture Start gate re-derives). `finding_options` rides inside the already-persisted assessment via the migrator's wholesale assessment cast. In history, a tapped finding serializes verbatim as a user turn (`buildTurnHistory`), satisfying the terminal-user guarantee.

**Out of scope (Step 1):** photo capture (only the `onAttachPhoto?` seam is reserved); the `CONFIRMED` confidence gate (the `STRONGLY_SUPPORTED` ceiling still holds — `CONFIRMED` is a later Stage-3 piece). Node gates: `lib/findingOptions.test.ts` (23), `server/scripts/verifyFindingOptions.js` (19), + finding cases added to `turnHistory.test.ts` and `diagnosticCases.test.ts`.

### Photo evidence — technician part photos (Stage 3, Step 1)

A technician can attach a **photo of a part** as evidence mid-diagnosis (worn pad, leaking pan, chafed wire); Claude reasons over it via Opus vision. Framing (enforced in the prompt): a photo is **corroborating evidence the brain reasons over, not a verdict** — it can raise a hypothesis on the ladder but the **ceiling stays `STRONGLY_SUPPORTED`** (no `CONFIRMED`, no confidence/schema change). It rides the existing `/api/diagnose-turn` loop — **no new endpoint**.

**Two entry points, one pipeline:** the **main Diagnose composer** (an attach button → stage a photo → Send ships text + photo as one turn) and the **`FindingCard` `onAttachPhoto` seam** (a photo staged against a directed inspection rides the next outcome tap → finding + photo in one turn). Capture is **mode-agnostic** (`lib/photoEvidence.ts`) so Ask Vulcan reuses it next.

**Pipeline:** `pickAndResize` (expo-image-picker → expo-image-manipulator, resize to ≤ ~1568 px / ~1.15 MP long edge, JPEG, base64) → `persistPhoto` (copy into `expo-file-system` `documentDirectory` for a durable thumbnail) → `sendUserMessage(text, image)` → the message carries `ChatMessage.image` (`{uri, mediaType, width?, height?}`, **base64 NEVER persisted**) → `runDiagnoseTurn` injects the transient base64 into the **final** user image turn of a local clone → `buildTurnHistory` keeps the image only on that final turn (else a text placeholder) → server `buildMessages` emits a standard Opus vision **image content block** (`server/diagnoseMessages.js` `buildTurnContent`; no header/flag/model change) for that one turn.

**The lean cost-in-history rule (load-bearing):** image bytes are sent to the model **once**, on the attach turn (final user turn, base64 present); every later turn that photo is a **text placeholder** (`"… [photo attached]"`) and the brain's own prior visual read (which the prompt instructs it to state) carries the observation forward — exactly the captured-evidence precedent. Token math (Opus 4.6, $5/MTok): a photo is ~1,560 tok ≈ **$0.008**, bounded regardless of session depth; the naive re-send-every-turn pattern is ~6× and grows. The phone never re-uploads base64 after the attach turn.

**Persistence (local-only):** `sanitizeMessages` (`lib/diagnosticCasesCore.ts`) was updated to **preserve + validate** `ChatMessage.image` through `migrateCase` (uri/mediaType strings, jpeg-only; a malformed image is dropped WITHOUT dropping the message; base64 is never restored; never throws). The thumbnail survives same-install resume; a dangling URI (reinstall/OS purge) renders a **`📷 photo (not available)` placeholder**, and **brain correctness is unaffected** (the visual read already lives in the persisted text). The **`DiagnosticRecord` strips `image`** from the saved conversation (local URIs are install-scoped). Cloud storage is **deferred** to the parked auth work (the `image.uri` field can later hold a remote URL).

**Prompt:** `UNIFIED_PHOTO_SECTION` in `server/assessPrompt.js`, composed into `UNIFIED_BODY` **only** (shared spine untouched → `verifyAssessPrompt.js` still PASS): reason over the photo like a finding/capture, **state what you actually see** (so the lean-history carry-forward holds), be honest about what a photo can't show, no verdict, ceiling unchanged.

**⚠️ Rebuild, not OTA:** `expo-image-manipulator` + `expo-file-system` are **native modules** — Photo Evidence Step 1 shipped via a fresh **`eas build --profile preview`**, NOT `eas update`. After that binary is installed, future JS-only photo tweaks can go back to `eas update`. (Note for the Ask Vulcan photo follow-on: the binary already carries these natives, so Ask Vulcan reuse is OTA-able.) `express.json` limit bumped to **6mb** (one resized base64 image + history per request).

**Ask Vulcan photo support (shipped OTA — reuses everything above):** a tech attaches a reference photo to an Ask question. **Reused wholesale:** `lib/photoEvidence.ts`, `ChatMessage.image`, `server/diagnoseMessages.js` `buildTurnContent`/`lastUserIndex`, the shared `components/PhotoThumb.tsx` (extracted from `diagnose.tsx` — both modes import it; Diagnose render unchanged), and the lean once-then-placeholder pattern. **Ask-specific work, two pieces:** (1) the `/api/ask` handler's message map (`server/index.js`, the `messages.map(...)` into `runAskToolLoop`) became image-aware via the same `buildTurnContent`/`lastUserIndex`; the image rides across the tool loop's iterations (the loop replays `convo` each iteration — bounded by `MAX_ASK_TOOL_ITERATIONS` (3) + 1). (2) **THE LOAD-BEARING CACHE BYPASS:** the Ask response cache keys on text only (`buildCacheKey(vehicle, lastUserText, ASK_MODEL)`), so an image-bearing ask is **excluded from cache read AND write** via `!messageHasImage(lastUserMsg)` added to `cacheEligible` (mirrors the `isSpecShapedQuestion` exclusion) — without it, a photo question could be served a stale text answer or have its photo-specific answer frozen for 30 days. A one-line `ASK_SYSTEM_PROMPT` bullet frames a tech photo as reference evidence (no Ask-side byte-equality guard exists). **Ask does NOT persist conversations** (in-memory `useState`), so there is **no `sanitizeMessages`/migrator work** in Ask. **OTA-able** — no new native dep. Node gate: `server/scripts/verifyAskCacheBypass.js` (11). **Known gap (not built):** the Ask→Diagnose handoff carries only `symptom` text, so a photo does not transfer across modes. Cloud storage still deferred to the auth work.

### Diagnostic case save/resume (Stage 2B)

The "patient chart": a versioned, auto-saved record of a diagnostic session so a tech can pause, switch cars, and resume later. **Mobile-only — no server, endpoint, schema, or AI-behavior change.** Resume re-sends the saved conversation history to `/api/diagnose` exactly as an in-session continuation does. This is the persistence layer Stage 2C's evidence loop writes into, which is why the **data model is the load-bearing part**.

**The envelope (`lib/diagnosticCasesCore.ts`, `DiagnosticCaseV1`).** Holds: thread (`messages` — assistant content is JSON `AssistantTurn` as everywhere; `assessments` as `SavedAssessmentEntry[]` = result + anchor only), intake (`complaint`/`mileage`/`operatingCondition`), `vehicle` (identity + VIN + source as history), timestamps, `status`/`closeReason`, `linkedRecordIds`, `loggerSessionIds`. **Reserved-but-empty 2C slots:** `evidenceLedger: EvidenceCaptureEntry[]` (shaped on `RequestedDataItem[]` + `DiagnosticSnapshot` — the exact objects 2C's capture executor will hold) and `caseState: CaseStateSlot | null` (mirrors `Hypothesis`). **The envelope carries NO live-connection data by type** (no `PidDescriptor[]`, no DTC arrays, no `FreezeFrame`, no snapshot inputs). That absence is the structural form of the history-vs-live split — a restored case literally cannot feed the auto-assess gate because the fields don't exist (the type-level version of the cleared-codes-handoff discipline).

**Versioning / migration (a hard gate).** Every envelope carries `schemaVersion` (currently 1). `migrateCase(raw)` is tolerant by contract: malformed / truncated / unversioned / **future-version (newer than this app)** input → `null`, NEVER throws. A future-version body (e.g. after an OTA rollback) returns null AND `loadCase` leaves it untouched on disk — the **never-deletes** invariant, so a later compatible app version can still read it. Per-case body keys (not one array) make this safe: a whole-array write can't clobber an unknown body. The 70-assertion `lib/diagnosticCases.test.ts` is the gate — Batch 1 wasn't "done" until malformed/truncated/`schemaVersion:99` fixtures all proved never-throws + never-deletes.

**Storage (`lib/diagnosticCases.ts`).** Per-device AsyncStorage: `vulcan:cases:case:<id>:v1` per body + `vulcan:cases:index:v1` (lightweight `CaseIndexEntry[]` for the list + VIN lookup, no body loads). A **serialized write queue** prevents auto-save's read-modify-write of the index from interleaving. Self-healing: a missing/corrupt index rebuilds from bodies. **Scalability:** per-device, cap-bounded at 25 — does NOT scale to multi-device/shared-shop; **server-side case sync is the post-auth migration path** (this AsyncStorage layer becomes an offline cache when auth + the backend user model land). Flagged in the module header.

**Keying + resume-by-VIN.** Each case has its own generated id (primary key). VIN is an **index, not the key** — one VIN maps to many cases (same truck, different problems, weeks apart); no-VIN cases (manual / pre-2008) exist and live in the manual list only. `findOpenCasesByVin` returns OPEN cases on a VIN; multiple → the tech picks from the filtered list.

**Auto-save grain.** No save button. Six triggers, once per completed turn, via the serialized queue: intake submit (creates the case, cap-enforced), user turn (saved before the API call so a crash can't lose input), assistant turn, assessment resolve (running slots NEVER persisted — an in-flight call can't survive process death), confirm (close+link), reject (link, stays open). `messagesRef`/`assessmentsRef` mirror the volatile arrays so each save reads fresh values, not stale state.

**The different-vehicle guard (the safety core).** The ONE place the resume-eligibility rule lives. `liveVehicleMatchesCase()` (render-time, derived) + a resume-time block, both reading `obd2.getConnectedVin()` — **ground truth from the OBD2 manager, NEVER the overridable VehicleContext**, so a manual vehicle override can't defeat it. The truth table (`liveAssessmentAllowed = canAutoAssess && liveVehicleMatchesCase()`):

| Session | Connection | Result |
|---|---|---|
| fresh | any | guard always true — 2A behavior untouched |
| resumed, has VIN | disconnected | resumes freely (no live car to mismatch) |
| resumed, has VIN | connected, VIN matches | resumes; live assessment allowed (live = case car) |
| resumed, has VIN | connected, VIN differs | **BLOCKED** — conversation never opens (resume-time) / live path disabled + banner (mid-session) |
| resumed, NO VIN | connected | live-assessment path **hard-suppressed** (can't prove same car) — closes the no-VIN leak structurally |
| resumed, NO VIN | disconnected | resumes freely |

There is no state where the live connection is one vehicle and the open conversation is another — the autopilot "reasoning over the wrong car's live data" failure is structurally impossible, not merely UI-gated.

**Lifecycle.** `open`/`closed`. Confirm-fix → `closeCase(id,"fix_confirmed",recordId)` (the DiagnosticRecords tie-in / confirmed-fix DB feed; shown as a distinct **FIXED** chip vs a user-close **CLOSED** chip — `closeReason` rides on the index entry). Reject links the record but keeps the case open. Cap 25: prune oldest CLOSED first; **all-25-open requires explicit consent** — an honest three-way prompt (delete the named oldest open case / continue unsaved / cancel), because closing can't free a cap slot (only deletion can) and a "close" label that silently deleted would be dishonest. Reopen of a closed case is deferred to 2C. `resetSession` leaves the prior case open + resumable and starts the next intake as a fresh id; its connection-aware vehicle branch is unchanged from 2A.

**Entry points.** Manual: the saved-cases list on the intake screen (renders from the index; tap = resume/view; close/delete actions). Automatic: `CaseResumePrompt` (root-mounted) offers resume when a connecting VIN matches open case(s). Both go through one shared, block-checked `attemptResume` that also drains pending escalation / `to_diagnose` handoffs, so a resume always wins over a stale prefill.

### Safety discipline (non-negotiable)

Claude may apply diagnostic logic freely from its training. It may NOT state specific numeric factory specifications (torque, pressures, capacities, expected sensor ranges) unless that value was injected in the verified data blocks. Any needed but unavailable spec must be listed in `unverified_specs_needed` with parameter name and purpose. This rule is in the system prompt and enforced by making `unverified_specs_needed` a required schema field.

## API Cost Measurement

Every Claude API call is instrumented on the server. Cost data is captured from `response.usage`, computed against verified per-token rates, and persisted to the Railway Volume.

### Pricing config (`server/costConfig.js`)

Single file with all per-token rates. **Verify rates before making pricing decisions** — the file header shows the last-verified date and source URL. Update the date when rates change.

| Model | Input | Cache Write (5m) | Cache Read | Output |
|-------|-------|-----------------|------------|--------|
| claude-opus-4-6 | $5/MTok | $6.25/MTok | $0.50/MTok | $25/MTok |
| claude-sonnet-4-6 | $3/MTok | $3.75/MTok | $0.30/MTok | $15/MTok |
| claude-haiku-4-5-20251001 | $1/MTok | $1.25/MTok | $0.10/MTok | $5/MTok |

### Cost logger (`server/costLogger.js`)

- **Per-call**: `logApiCost(usage, model, { sessionId, callType })` — fire-and-forget, zero latency impact
- **Aggregates**: today / this week / all-time, by call type, by model
- **Cost breakdown**: uncached input vs cache-write vs cache-read vs output (tells you which optimisation lever matters)
- **Persists**: `costEntries.json` (last 500 calls) + `costAggregate.json` on the Railway Volume
- **Startup log**: prints today's cost + all-time total on every Railway deploy
- **Periodic log**: prints a summary line to Railway logs every 10 API calls

### Reading the results

**Railway logs** (during a session): look for `[cost]` lines showing per-call breakdown and `[cost] summary` lines every 10 calls.

**Aggregate endpoint**: `GET /api/costs/summary` returns the full aggregate + per-session breakdown + 50 most recent calls. Example:
```
curl https://vulcan-backend-production.up.railway.app/api/costs/summary | python -m json.tool
```

**On-device diagnostic log** (shop testing): every Smart Diagnose assessment entry shows the full cost breakdown. Open Diagnostic Log → expand a session → expand an assessment entry to see tokens and dollar cost.

**Cost breakdown fields explained:**
- `input`: tokens NOT served from cache — full price
- `cacheWrite`: tokens written to 5-min ephemeral cache — 1.25× input price (one-time cost)
- `cacheRead`: tokens served from cache — 0.1× input price (very cheap)
- `output`: generated tokens — most expensive per token at Opus pricing

If `cacheRead` is large relative to `input`, caching is working well. If `output` dominates, model tiering (Haiku for lightweight steps) is the lever to pull.

### Call types logged

| callType | Endpoint | Model |
|----------|----------|-------|
| `assessment` | `/api/assess` (Smart Diagnose) | claude-opus-4-6 |
| `evidence-update` | `/api/evidence-update` (Stage 2C-3 evidence loop) | claude-opus-4-6 |
| `diagnose-turn` | `/api/diagnose-turn` (SB3 unified diagnostic brain) | claude-opus-4-6 |
| `diagnose` | `/api/diagnose` (conversational Diagnose) | claude-opus-4-6 |
| `ask-vulcan` | `/api/ask` | claude-opus-4-6 |
| `dtc-fallback` | background DTC lookup | claude-sonnet-4-6 |

All call types feed the same server-side cost aggregate (`/api/costs/summary`), bucketed under `byType` (so the evidence loop's per-session cost is visible separately from one-shot assessments).

The on-device diagnostic log also captures cost for `assessment`, `ask_vulcan`, and `diagnose_turn` entry types. Each shows the full token + dollar breakdown when expanded. DTC fallback costs are captured in the server aggregate only (the DTC endpoint returns the entry without cost; fallback fires rarely and is not high-volume).

`/api/diagnose` and `/api/ask` both return `cost` in their response bodies so the mobile app can log per-turn cost. Early-return paths in `/api/ask` (DTC direct-answer, spec direct-answer, cache hit) return `cost: null` since no Claude call was made.

## Current Development Priorities

- **Photo Evidence in Ask Vulcan — COMPLETE (shipped OTA via `eas update`; no rebuild — natives already in the binary).** Reuses `lib/photoEvidence.ts` / `ChatMessage.image` / `buildTurnContent` / the shared `components/PhotoThumb.tsx`. Ask-specific: the image-aware `/api/ask` message build + the **load-bearing cache bypass** for image-bearing asks (`!messageHasImage` in `cacheEligible`) + one `ASK_SYSTEM_PROMPT` bullet. Ask doesn't persist conversations → no migrator work. See Diagnostic Engine Architecture → Photo evidence → Ask Vulcan photo support. **On-device watch items:** the Ask thumbnail, and confirming a photo question is genuinely NOT served from / written to cache. **Known gap:** Ask→Diagnose handoff drops the photo (text-only). Next: cloud storage (auth-gated) / iOS audit.
- **Photo Evidence Step 1 — COMPLETE (server + mobile; shipped via `eas build --profile preview`, NOT OTA — native deps).** Technicians attach a part photo from the Diagnose composer (anytime) or the FindingCard seam; Opus reasons over it as corroborating evidence (ceiling stays `STRONGLY_SUPPORTED`). Lean cost-in-history (image bytes sent once → text placeholder after; ≈$0.008/photo, bounded). Local-only storage (`ChatMessage.image`, base64 never persisted; `sanitizeMessages` preserves it; record strips it). See Diagnostic Engine Architecture → Photo evidence. **On-device watch items:** thumbnail render, resume with a missing file (placeholder, not crash), and the brain stating what it sees so the carry-forward holds. **Next:** Ask Vulcan photo support (separate pass; the binary already carries the natives → OTA-able). Cloud photo storage deferred to the auth work.
- **Stage 3 Step 1 — guided result-capture COMPLETE (server + mobile, awaiting on-truck validation).** A directed bounded physical inspection now carries 2–4 brain-authored `finding_options` rendered as glove-friendly tap buttons; the tapped finding feeds the existing `/api/diagnose-turn` loop. Additive + fail-soft; open-question-no-buttons is structural; byte-equality guard untouched; `CONFIRMED` ceiling held; photo attach-point seam left (not built). See Diagnostic Engine Architecture → Guided result-capture. **On-truck watch items:** the "couldn't check" adaptation (brain should propose an alternative, not assume), and any tendency to fake-bound an open question into buttons. Remaining Stage 3: adaptive-stance UI, the `CONFIRMED` confirmation gate, guided checklists.
- **Unified diagnostic flow (SB1–SB4) — COMPLETE + hardware-validated (2026-06-14).** The `/diagnose` thread runs on the unified `/api/diagnose-turn` brain (talk / request-a-capture / conclude, one move per turn); Claude-directed live monitoring is delivered through it. **Deferred polish (not blocking):** baseline-poll-on-connect — start a light passive PID poll when a vehicle connects so a Diagnose-first session has a live snapshot immediately (today a connected start with an empty snapshot asks one operating-condition question before its first capture; a baseline would make it capture turn-1). See Auto-reconnect deferred.
- **Offline resilience ⚠️ PRIORITY (surfaced live during SB4 truck testing)** — graceful VIN-decode-failure handling + local OBD2 buffering; the durable fix is the self-hosted NHTSA vPIC decoder (Pre-launch infrastructure). See Known Gaps → Offline resilience.
- **iOS native cleanup** — Expo config plugin to exclude the Classic Bluetooth pod from iOS builds (see Known Platform Issues)
- **Pre-launch infrastructure** — Supabase/Postgres data layer (foundation connected + schema in place; remaining: extraction pipeline, then migrate cache data off the JSON files); auth; billing; analytics; error/crash tracking (Sentry or similar)
- **TestFlight beta + App Store launch prep**
